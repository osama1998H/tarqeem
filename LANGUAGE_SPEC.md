<div dir="rtl" align="right">

# مواصفات لغة ترقيم

**الإصدار ١.٠.٠**

---

## فهرس المحتويات

١. [مقدمة](#١-مقدمة)
٢. [غاية اللغة](#٢-غاية-اللغة)
٣. [فلسفة التصميم](#٣-فلسفة-التصميم)
٤. [البنية اللغوية](#٤-البنية-اللغوية)
٥. [نظام الأنماط](#٥-نظام-الأنماط)
٦. [التعبيرات](#٦-التعبيرات)
٧. [الجمل](#٧-الجمل)
٨. [الدوال](#٨-الدوال)
٩. [البرمجة الكائنية](#٩-البرمجة-الكائنية)
١٠. [الوحدات والاستيراد](#١٠-الوحدات-والاستيراد)
١١. [معالجة الاستثناءات](#١١-معالجة-الاستثناءات)
١٢. [التزامن](#١٢-التزامن)
١٣. [نموذج الذاكرة](#١٣-نموذج-الذاكرة)
١٤. [المكتبة القياسية](#١٤-المكتبة-القياسية)
١٥. [القواعد النحوية الرسمية](#١٥-القواعد-النحوية-الرسمية)
١٦. [ملحق: مرجع الكلمات المفتاحية](#ملحق-أ-مرجع-الكلمات-المفتاحية)

---

## ١. مقدمة

ترقيم هي لغة برمجة مُترجَمة ذات أنماط ثابتة للأغراض العامة مع دعم كامل للصياغة العربية. اسم «ترقيم» يعكس غرض اللغة في جعل البرمجة متاحة للناطقين بالعربية.

### ١.١ نطاق المواصفات

تُعرِّف هذه المواصفات:
- البنية اللغوية والنحوية لبرامج ترقيم
- نظام الأنماط وقواعد التحقق منها
- دلالات التعبيرات والجمل والإعلانات
- نموذج البرمجة الكائنية
- نظام الوحدات والاستيراد
- آليات معالجة الاستثناءات
- نموذج الذاكرة ودلالات دورة الحياة

### ١.٢ اصطلاحات الترميز

- `كلمة_مفتاحية` - الكلمات المفتاحية بخط ثابت
- `<اسم>` - موضع لأسماء يحددها المستخدم
- `[اختياري]` - عناصر اختيارية بين أقواس معقوفة
- `...` - تكرار العنصر السابق
- `|` - اختيارات بديلة

---

## ٢. غاية اللغة

### ٢.١ بيان المشكلة

هيمنت اللغات المبنية على الإنجليزية على البرمجة تاريخياً، مما خلق حواجز للناطقين بالعربية:
- تعلم البرمجة يتطلب تعلم المصطلحات الإنجليزية
- لا يمكن للكود التعبير عن مفاهيم المجال باللغة الأم
- معالجة النص من اليمين لليسار تأتي كفكرة لاحقة

### ٢.٢ الحل

تعالج ترقيم هذه التحديات بتوفير:

١. **صياغة عربية أصيلة**: جميع الكلمات المفتاحية لها أشكال عربية أساسية
٢. **تصميم يونيكود أولاً**: دعم كامل لـ UTF-8 مع معالجة صحيحة للنص ثنائي الاتجاه
٣. **أداء مُترجَم**: كود آلة أصلي عبر واجهة LLVM الخلفية
٤. **ميزات حديثة**: أنماط ثابتة، أنماط معممة، متوازي/انتظر، مطابقة الأنماط

### ٢.٣ الجمهور المستهدف

- المطورون الناطقون بالعربية الذين يتعلمون البرمجة
- فرق التطوير في المناطق الناطقة بالعربية
- المؤسسات التعليمية التي تدرس البرمجة بالعربية
- المشاريع التي تتطلب مصطلحات المجال العربية في الكود

---

## ٣. فلسفة التصميم

### ٣.١ المبادئ الأساسية

| المبدأ | الوصف |
|--------|-------|
| **العربية أولاً** | العربية هي اللغة الأساسية |
| **الأمان** | الأنماط الثابتة القوية تمنع أخطاء وقت التشغيل |
| **التعبيرية** | ميزات لغوية حديثة لكود موجز |
| **السرعة** | تُترجَم إلى كود أصلي للأداء الإنتاجي |

### ٣.٢ مصادر الإلهام

تستلهم ترقيم من:

- **بايثون**: صياغة نظيفة وقابلة للقراءة
- **تايب سكريبت**: أنماط ثابتة مع استنتاج
- **رست**: مفاهيم أمان الذاكرة، مطابقة الأنماط
- **جافاسكريبت**: متوازي/انتظر الحديثة، الدوال السهمية

### ٣.٣ المقايضات

| الاختيار | المقايضة |
|----------|----------|
| الأنماط الثابتة | أكثر تفصيلاً لكنها تكتشف الأخطاء وقت الترجمة |
| عد المراجع | أبسط من جامع القمامة، حتمي، لكن المراجع الدائرية تحتاج عناية |
| مُترجَمة | تكرار أبطأ لكن تشغيل أسرع |
| الرؤية الصريحة | كتابة أكثر لكن نية أوضح |

---

## ٤. البنية اللغوية

### ٤.١ ترميز الملفات المصدرية

- يجب أن تكون الملفات المصدرية مُرمَّزة بـ UTF-8
- تخضع المُعرِّفات لتطبيع NFC قبل المقارنة
- امتداد الملف: `.ترقيم`

### ٤.٢ المُعرِّفات

المُعرِّفات **عربية فقط**. الحروف اللاتينية مرفوضة صراحةً في مرحلة التحليل اللغوي — ترقيم لغة برمجة عربية أصيلة، لا لغة إنجليزية بقناع عربي:

```
معرِّف := بداية_معرِّف تكملة_معرِّف*
بداية_معرِّف := <حرف عربي> | '_'
تكملة_معرِّف := <حرف عربي> | <رقم> | <تشكيل> | '_' | <تطويل>
```

حيث:
- `<حرف عربي>`: أي حرف من كتل يونيكود العربية (العربية الأساسية، الملحق العربي، العربية الموسعة-أ، أشكال العرض العربية أ/ب)
- `<رقم>`: أرقام لاتينية (٠-٩ بالشكل `0-9`) أو أرقام عربية-هندية (`٠-٩`)
- `<تشكيل>`: علامات التشكيل (U+064B–U+0652)
- `<تطويل>`: الكشيدة (U+0640)

معرِّفات صالحة:
```tarqeem
متغير           // معرِّف عربي
مستخدم_١        // ممزوج مع شرطة سفلية ورقم
_خاص            // يبدأ بشرطة سفلية
رقم1            // أرقام لاتينية مسموحة داخل المعرِّف
```

معرِّفات **غير** صالحة:
```tarqeem
userName        // ❌ خطأ [ق٠٠٠١]: المعرفات الإنجليزية غير مسموح بها
x               // ❌ حرف لاتيني
```

### ٤.٣ الكلمات المفتاحية

#### كلمات المتغيرات
| الكلمة المفتاحية | الوصف |
|------------------|-------|
| `متغير` | متغير قابل للتعديل |
| `ثابت` | ثابت غير قابل للتعديل |

#### كلمات الدوال
| الكلمة المفتاحية | الوصف |
|------------------|-------|
| `دالة` | إعلان دالة |
| `أرجع` / `ارجع` | جملة الإرجاع |
| `متوازي` | دالة متوازية |
| `انتظر` | تعبير الانتظار |

#### كلمات التحكم في التدفق
| الكلمة المفتاحية | الوصف |
|------------------|-------|
| `إذا` / `اذا` | شرط |
| `وإلا` / `والا` | فرع وإلا |
| `طالما` | حلقة طالما |
| `لكل` | حلقة لكل |
| `في` | عامل في |
| `افعل` | حلقة افعل-طالما |
| `أوقف` / `اوقف` | إيقاف الحلقة |
| `استمر` | استمرار الحلقة |
| `تطابق` | مطابقة الأنماط |
| `حالة` | حالة المطابقة |
| `غير_ذلك` | الحالة الافتراضية |

#### كلمات البرمجة الكائنية
| الكلمة المفتاحية | الوصف |
|------------------|-------|
| `صنف` | إعلان صنف |
| `ميثاق` | إعلان ميثاق (عقد) |
| `يرث` | الوراثة |
| `يلتزم` | الالتزام بالميثاق |
| `عام` | رؤية عامة |
| `خاص` | رؤية خاصة |
| `محمي` | رؤية محمية |
| `مشترك` | عضو مشترك بين النسخ |
| `منشئ` | المنشئ |
| `هذا` | مرجع النسخة الحالية |
| `الأصل` / `الاصل` | مرجع الأب |
| `جديد` | إنشاء كائن |

#### كلمات معالجة الاستثناءات
| الكلمة المفتاحية | الوصف |
|------------------|-------|
| `حاول` | كتلة المحاولة |
| `التقط` | كتلة الالتقاط |
| `أخيراً` / `اخيرا` | كتلة أخيراً |
| `ارمِ` / `ارم` | رمي استثناء |

#### كلمات الوحدات
| الكلمة المفتاحية | الوصف |
|------------------|-------|
| `استورد` | استيراد وحدة |
| `صدّر` / `صدر` | تصدير إعلان |
| `من` | محدد المصدر |
| `كـ` / `ك` | محدد الاسم المستعار |

#### الكلمات الحرفية
| الكلمة المفتاحية | الوصف |
|------------------|-------|
| `صحيح` | صواب منطقي |
| `خطأ` / `خطا` | خطأ منطقي |
| `لا_شيء` | قيمة فارغة |

#### العوامل المنطقية (الشكل الكلمي)
| الكلمة المفتاحية | الوصف |
|------------------|-------|
| `و` | و المنطقية |
| `أو` / `او` | أو المنطقية |
| `ليس` | النفي المنطقي |

#### كلمات مفتاحية تصلح أسماءً

بعض الكلمات المفتاحية محجوزة في سياقها فقط، وتُقبَل كأسماء في المواضع التي لا
يمكن أن يأتي فيها غير اسم:

| الكلمات | تصلح اسماً لـ |
|---------|---------------|
| `احصل`، `عيّن`، `حالة` | أي معرِّف؛ محجوزة داخل كتل `خاصية` وأذرع `تطابق` فقط |
| أسماء الأنماط: `عدد`، `عدد_عشري`، `نص`، `منطقي`، `مصفوفة`، `قاموس`، `أي` | دالة، دالة عضو، حقل، خاصية، معامل، متغير، متغير `لكل … في`، واسم بعد `.` |
| `صحيح`، `خطأ` | حالة تعداد فقط |

```tarqeem
صنف مشغل {
    خاص نص: نص                      // حقل باسم نمط
    عام دالة عدد() -> عدد { أرجع ١ }  // دالة باسم نمط
}
صدّر دالة اطبع_نص(نص: نص) { اطبع(نص) }  // معامل باسم نمط

تعداد حالة_الاختبار {
    نجح،
    خطأ                              // حالة باسم قيمة منطقية
}
```

`صحيح`/`خطأ` مقصورتان على حالات التعداد لأن الحالة لا تُقرأ إلا عبر `::`
(`حالة_الاختبار::خطأ`)، فيبقى `خطأ` المجرَّد قيمةً منطقية. لذلك **لا يصلح**
`خطأ` اسماً لصنف: `جديد خطأ()` يقرأ المستدعى تعبيراً أولياً فيصير القيمة
المنطقية لا اسم نمط.

> **ملاحظة**: أسماء الأنماط `عدد` و`عدد_عشري` و`نص` و`منطقي` هي كذلك أسماء
> دوال تحويل مدمجة. تسميةُ دالةٍ أو متغيرٍ بأحدها مسموحة: الاسم المُعرَّف
> **يُظلِّل** المدمج، لأن المدمجات آخر مراتب البحث (راجع §٤.٩). فـ`دالة عدد()`
> تُعرِّف دالتك ويستدعيها كل نداء لـ`عدد` في نطاقها، و`متغير عدد = ٧` يجعل
> `عدد("٥")` بعدها في النطاق نفسه خطأ نوع — لأن `عدد` صار عدداً لا دالة. لا
> توجد بعد صيغة للوصول إلى المدمج بعد تظليله.

### ٤.٤ القيم الحرفية

#### الأعداد الصحيحة
```tarqeem
٤٢        // عشري بالأرقام العربية
42        // عشري
0x2A      // ست عشري
0b101010  // ثنائي
0o52      // ثماني
```

#### الأعداد العشرية
```tarqeem
٣.١٤      // بالأرقام العربية
3.14159
2.5e10
1.0E-5
```

#### النصوص
```tarqeem
"مرحبا"                   // علامات اقتباس مزدوجة
'مرحبا'                   // علامات اقتباس مفردة
«مرحبا»                   // علامات اقتباس عربية
"سطر أول\nسطر ثاني"       // تسلسلات الهروب
```

تسلسلات الهروب:
| التسلسل | المعنى |
|---------|--------|
| `\n` | سطر جديد |
| `\t` | مسافة جدولة |
| `\r` | إرجاع |
| `\\` | شرطة مائلة عكسية |
| `\"` | علامة اقتباس مزدوجة |
| `\'` | علامة اقتباس مفردة |

#### القيم المنطقية
```tarqeem
صحيح      // صواب
خطأ       // خطأ
```

#### القيمة الفارغة
```tarqeem
لا_شيء    // قيمة فارغة
```

#### المصفوفات
```tarqeem
[١، ٢، ٣]          // بالأرقام والفاصلة العربية
[1, 2, 3]          // بالأرقام والفاصلة اللاتينية
["أ"، "ب"، "ج"]    // مصفوفة نصوص
[]                 // مصفوفة فارغة
```

#### الكائنات
```tarqeem
{ اسم: "أحمد"، عمر: ٣٠ }
```

### ٤.٥ العوامل

#### العوامل الحسابية
| العامل | الوصف |
|--------|-------|
| `+` | جمع |
| `-` | طرح |
| `*` | ضرب |
| `/` | قسمة |
| `%` | باقي القسمة |
| `**` | الأس |

#### عوامل المقارنة
| العامل | الوصف |
|--------|-------|
| `==` | يساوي |
| `!=` | لا يساوي |
| `<` | أصغر من |
| `<=` | أصغر من أو يساوي |
| `>` | أكبر من |
| `>=` | أكبر من أو يساوي |

#### العوامل المنطقية
| العامل | الشكل العربي | الوصف |
|--------|--------------|-------|
| `&&` | `و` | و المنطقية |
| `\|\|` | `أو` | أو المنطقية |
| `!` | `ليس` | النفي المنطقي |

#### عوامل الإسناد
| العامل | الوصف |
|--------|-------|
| `=` | إسناد |
| `+=` | جمع وإسناد |
| `-=` | طرح وإسناد |
| `*=` | ضرب وإسناد |
| `/=` | قسمة وإسناد |
| `%=` | باقي وإسناد |

#### الزيادة والنقصان
| العامل | الوصف |
|--------|-------|
| `++` | زيادة |
| `--` | نقصان |

#### عوامل أخرى
| العامل | الوصف |
|--------|-------|
| `?:` | الشرطي الثلاثي |
| `?.` | السلسلة الاختيارية (مخطط) |
| `??` | دمج القيمة الفارغة (مخطط) |

### ٤.٦ علامات الترقيم

تُقبَل كل من علامات الترقيم العربية واللاتينية:

| لاتيني | عربي | الاستخدام |
|--------|------|-----------|
| `,` | `،` | فاصل |
| `;` | `؛` | منهي الجملة |
| `(` `)` | | تجميع |
| `{` `}` | | كتلة |
| `[` `]` | | فهرس/مصفوفة |
| `:` | | تحديد النمط |
| `->` | | نمط الإرجاع |
| `=>` | | الدالة السهمية |
| `.` | | الوصول للعضو |

### ٤.٦.١ نهاية الجملة والأسطر المتعددة

تنتهي الجملة بسطر جديد؛ الفاصلة المنقوطة (`؛` أو `;`) اختيارية. لذلك لا يمكن
متابعة تعبير على السطر التالي في المستوى الأعلى:

```tarqeem
متغير س = ١ +
          ٢          // ❌ خطأ: السطر الجديد أنهى الجملة
```

لكن **داخل قوس مفتوح** (`(` أو `[`) لا يُعَدّ السطر الجديد نهاية جملة، فيمكن
توزيع المعاملات والعناصر والتعبيرات على أسطر:

```tarqeem
// قائمة معاملات موزعة
دالة اجمع(
    أ: عدد،
    ب: عدد
) -> عدد { ... }

// استدعاء موزع
متغير مجموع = اجمع(
    ١،
    ٢
)

// قيمة مصفوفة موزعة
ثابت أرقام = [
    ١، ٢،
    ٣
]

// شرط طويل بين قوسين
إذا (مجموع > ٥ &&
     مجموع < ١٠) {
    ...
}

// تعبير موزع: أحطه بقوسين
أرجع (هذا.سنة == آخر.سنة &&
      هذا.شهر == آخر.شهر)
```

> **قيد حالي**: التعليقات داخل قائمة معاملات موزعة غير مدعومة —
> `ف(\n ١، // ملاحظة\n ٢)` خطأ. ضع التعليق فوق الجملة أو بعدها.

### ٤.٧ التعليقات

```tarqeem
// تعليق سطري

/* تعليق
   متعدد الأسطر */

/// تعليق توثيقي
/// يُرجع مجموع عددين
دالة جمع(أ: عدد، ب: عدد) -> عدد { ... }

/** تعليق توثيقي كتلي */
```

التعليقات تُقبَل بأي ترتيب قبل الإعلان — في المستوى الأعلى وداخل `صنف`
و`ميثاق` و`تعداد` — والتعليق التوثيقي **الأقرب** إلى الإعلان هو الذي يوثّقه:

```tarqeem
/// وثيقة الوحدة

// ═══════════════
// قسم العمليات
// ═══════════════

/// القيمة المطلقة لعدد
صدّر دالة مطلق(س: عدد) -> عدد { ... }
```

### ٤.٧.١ توثيق الملف

تعليق توثيقي يقع بعد `بسم_الله` مباشرة يوثّق **الملف** لا الإعلان الذي يليه،
ويظهر كوصف الوحدة في `tarqeem doc`. يُعَدّ توثيقاً للملف في هذه الحالات:

١. يليه تعليق توثيقي أقرب إلى الإعلان (فذاك يوثّق الإعلان)
٢. لا يليه شيء (`الحمد_لله` أو نهاية الملف)
٣. يليه `استورد` أو `صدّر * من` أو `صدّر { … }` — وهي لا تحمل توثيقاً

```tarqeem
بسم_الله

/// وحدة الرياضيات        ← توثيق الملف
///
/// @منذ ١.٠.٠

/// القيمة المطلقة        ← توثيق الدالة
صدّر دالة مطلق(س: عدد) -> عدد { ... }

الحمد_لله
```

أما إذا جاء الإعلان مباشرة بعد التعليق التوثيقي فهو توثيق ذلك الإعلان، لا الملف.

> **قيد**: قائمة وصول الخاصية (`احصل`/`عيّن` داخل `خاصية`) لا تحمل توثيقاً،
> فتعليق توثيقي قبل `احصل` خطأ — ضع التوثيق على `خاصية` نفسها. وتوثيق دالة
> الميثاق أو حالة التعداد يُحفَظ، أما التعليقات العادية في نفس المجموعة فلا
> يحتفظ بها التمثيل.

### ٤.٨ أولوية العوامل

من الأدنى إلى الأعلى أولوية:

| المستوى | العوامل | الترابط |
|---------|---------|---------|
| ١ | `=`، `+=`، `-=`، `*=`، `/=`، `%=` | يمين |
| ٢ | `?:` (الثلاثي) | يمين |
| ٣ | `\|\|`، `أو` | يسار |
| ٤ | `&&`، `و` | يسار |
| ٥ | `==`، `!=` | يسار |
| ٦ | `<`، `<=`، `>`، `>=` | يسار |
| ٧ | `+`، `-` | يسار |
| ٨ | `*`، `/`، `%` | يسار |
| ٩ | `**` | يمين |
| ١٠ | `!`، `-` (أحادي)، `++`، `--` | يمين |
| ١١ | `()`، `[]`، `.` | يسار |

### ٤.٩ ترتيب البحث عن الأسماء

يُبحَث عن الاسم في المراتب التالية، و**أول تطابق يفوز**:

| المرتبة | الموضع |
|---------|--------|
| ١ | المتغيرات والدوال المحلية |
| ٢ | النطاقات المحيطة |
| ٣ | أسماء الوحدة (المستوى الأعلى) |
| ٤ | الأسماء المستوردة |
| ٥ | الدوال المدمجة |

المدمجات **آخر** المراتب، لا كلمات محجوزة. فتسمية دالة أو متغير باسم مدمج
مسموحة، والمُعرَّف يُظلِّل المدمج:

```tarqeem
دالة طول(س: نص) -> عدد {
    أرجع ٤٢
}

اطبع(طول("مرحبا"))   // ٤٢ — دالتك، لا المدمجة
```

وكذلك الاستيراد (المرتبة ٤) يُظلِّل المدمج (المرتبة ٥):

```tarqeem
استورد { نوع } من "./وحدتي"
اطبع(نوع(٥))          // دالة الوحدة، لا المدمجة
```

> **قيد حالي**: المرتبتان ٣ و٤ لا تتظلَّلان بعد. إعلانٌ في الملف الرئيسي يحمل
> اسم إعلانٍ في وحدة مستوردة من قرص يُنتج الخطأ
> [و٠١٠١](docs/رموز_الأخطاء/و/و٠١٠١.md) عند الدمج، لا تظليلاً.

**المرتبة ٤ هي ما استوردتَه أنت، لا كل ما تُعلنه الوحدة.** فوحدةٌ تُصدِّر
`شيء` و`اطبع` معاً، استوردتَ منها `شيء` وحدها، لا يُظلِّل `اطبع`ها المدمجة:

```tarqeem
استورد { شيء } من "./وحدتي"
اطبع(٥)                // المدمجة — «اطبع» الوحدة لم تُستورَد
```

> **قيد حالي**: لا توجد بعد صيغة للوصول إلى المدمج بعد تظليله، فدالة تُنادي
> المدمج بنفس اسمها تُصبح استدعاءً ذاتياً لا نهائياً:
> `دالة مطلق(س) { أرجع مطلق(س) }`. استخدم اسماً مختلفاً للغلاف حتى تُضاف صيغة
> صريحة.

---

## ٥. نظام الأنماط

### ٥.١ نظرة عامة

تستخدم ترقيم **نظام أنماط ثابت قوي** مع استنتاج الأنماط. تُفحَص الأنماط وقت الترجمة، مما يمنع كثيراً من أخطاء وقت التشغيل.

### ٥.٢ الأنماط الأولية

| النمط | الوصف | الحجم |
|-------|-------|-------|
| `عدد` | عدد صحيح بإشارة | ٦٤ بت |
| `عدد_عشري` | عدد عشري عائم | ٦٤ بت |
| `نص` | سلسلة UTF-8 | متغير |
| `منطقي` | قيمة منطقية | ١ بت |
| `لا_شيء` | قيمة فارغة | مؤشر |

> **ملاحظة**: الدوال التي لا تُرجع قيمة تحذف ببساطة تحديد نمط الإرجاع. لا توجد كلمة `فراغ`.

### ٥.٣ الأنماط المركبة

#### نمط المصفوفة
```tarqeem
مصفوفة<عدد>        // مصفوفة أعداد صحيحة
مصفوفة<نص>         // مصفوفة نصوص
```

#### نمط القاموس
```tarqeem
قاموس<نص، عدد>     // قاموس من نص إلى عدد
```

#### النمط الاختياري
```tarqeem
عدد?               // عدد اختياري (قد يكون فارغاً)
نص?                // نص اختياري
```

#### نمط الدالة
```tarqeem
(عدد، عدد) -> عدد   // دالة تأخذ عددين وتُرجع عدداً
()                  // دالة بدون معاملات ولا إرجاع
() -> عدد           // دالة بدون معاملات تُرجع عدداً
```

العامل `->` يرتبط من اليمين، لذا يمكن تكديس أنماط الدوال (curry):
```tarqeem
(عدد) -> (عدد) -> عدد   // ← (عدد) -> ((عدد) -> عدد)
```

> **ملاحظة**: نمط الدالة لا يمكن تمييزه بعد كنمط اختياري (`(عدد) -> عدد?`
> يُفسَّر على أن `?` تُطبَّق على نمط الإرجاع فقط، وليس على نمط الدالة ككل).

### ٥.٤ الأنماط المُعرَّفة من المستخدم

#### أنماط الأصناف
```tarqeem
صنف شخص { ... }
متغير ش: شخص       // متغير من نمط شخص
```

#### أنماط المواثيق
```tarqeem
ميثاق قابل_للطباعة { ... }
```

#### الأنماط المعممة
```tarqeem
صنف قائمة<ن> { ... }
متغير أرقام: قائمة<عدد>
```

### ٥.٥ الأنماط الخاصة

| النمط | الوصف |
|-------|-------|
| `أي` | يقبل أي نمط (مخرج طوارئ) |
| `أبداً` | الدالة لا تُرجع أبداً |
| `مجهول` | يُستخدم أثناء الاستنتاج |

### ٥.٦ توافق الأنماط

#### التحويلات الضمنية
- `عدد` ← `عدد_عشري` (من صحيح إلى عشري)
- `ن` ← `ن?` (من قيمة إلى اختيارية)
- `لا_شيء` ← `ن?` (من فارغ إلى اختيارية)

#### قسر دمج النصوص
عند استخدام `+` مع نص، تُحوَّل الأنماط الأخرى ضمنياً:
```tarqeem
"العدد: " + ٤٢        // ← "العدد: ٤٢"
"القيمة: " + صحيح     // ← "القيمة: صحيح"
```

### ٥.٧ استنتاج الأنماط

يمكن استنتاج الأنماط من التهيئة:
```tarqeem
متغير س = ٥          // س: عدد
متغير اسم = "أحمد"    // اسم: نص
ثابت قيم = [١، ٢، ٣]  // قيم: مصفوفة<عدد>
```

### ٥.٨ تحديد الأنماط

تحديد الأنماط الصريح يستخدم صياغة النقطتين:
```tarqeem
متغير س: عدد = ٥
ثابت ط: عدد_عشري = ٣.١٤
متغير قائمة: مصفوفة<نص> = []
```

---

## ٦. التعبيرات

### ٦.١ التعبيرات الأولية

```tarqeem
٤٢                    // قيمة عدد صحيح
٣.١٤                  // قيمة عدد عشري
"مرحبا"               // قيمة نص
صحيح                  // صواب منطقي
خطأ                   // خطأ منطقي
لا_شيء                // قيمة فارغة
اسم_المتغير           // معرِّف
(تعبير)               // تجميع
```

### ٦.٢ التعبيرات الحسابية

```tarqeem
أ + ب                 // جمع
أ - ب                 // طرح
أ * ب                 // ضرب
أ / ب                 // قسمة
أ % ب                 // باقي القسمة
أ ** ب                // الأس
-أ                    // نفي
```

### ٦.٣ تعبيرات المقارنة

```tarqeem
أ == ب                // يساوي
أ != ب                // لا يساوي
أ < ب                 // أصغر من
أ <= ب                // أصغر من أو يساوي
أ > ب                 // أكبر من
أ >= ب                // أكبر من أو يساوي
```

### ٦.٤ التعبيرات المنطقية

```tarqeem
أ && ب                // و المنطقية
أ و ب                 // الشكل العربي
أ || ب                // أو المنطقية
أ أو ب                // الشكل العربي
!أ                    // النفي المنطقي
ليس أ                 // الشكل العربي
```

### ٦.٥ تعبيرات الإسناد

```tarqeem
س = ٥                 // إسناد بسيط
س += ١                // جمع وإسناد
س -= ١                // طرح وإسناد
س *= ٢                // ضرب وإسناد
س /= ٢                // قسمة وإسناد
س++                   // زيادة لاحقة
++س                   // زيادة سابقة
س--                   // نقصان لاحق
--س                   // نقصان سابق
```

### ٦.٦ التعبير الثلاثي

```tarqeem
شرط ? قيمة_صحيح : قيمة_خطأ

// مثال — «حالة» كلمة سياقية: محجوزة داخل «تطابق» فقط، وتصلح اسماً لمتغير
متغير حالة = عمر >= ١٨ ? "بالغ" : "قاصر"
```

### ٦.٧ الوصول للعضو

```tarqeem
كائن.خاصية            // الوصول للخاصية
كائن.دالة()           // استدعاء الدالة
```

### ٦.٨ الوصول بالفهرس

```tarqeem
مصفوفة[٠]             // فهرس المصفوفة
قاموس["مفتاح"]        // الوصول بمفتاح القاموس
```

### ٦.٩ استدعاء الدالة

```tarqeem
دالة()                // بدون معاملات
دالة(أ، ب)            // مع معاملات
كائن.دالة(أ)          // استدعاء دالة عضو
```

### ٦.١٠ إنشاء الكائن

```tarqeem
جديد صنف()            // استدعاء المنشئ
جديد صنف(أ، ب)        // مع معاملات
```

### ٦.١١ تعبيرات لامدا

```tarqeem
// جسم تعبيري
(س) => س * ٢

// جسم كتلي
(س، ص) => {
    متغير نتيجة = س + ص
    أرجع نتيجة
}

// معاملات محددة النمط
(س: عدد، ص: عدد) => س + ص
```

### ٦.١٢ تعبيرات المصفوفة

```tarqeem
[١، ٢، ٣]             // قيمة مصفوفة
[]                    // مصفوفة فارغة
```

### ٦.١٣ تعبير الانتظار

```tarqeem
انتظر وعد             // انتظار وعد
انتظر دالة_متوازية()
```

---

## ٧. الجمل

### ٧.١ إعلان المتغير

```tarqeem
// متغير قابل للتعديل
متغير اسم = "أحمد"
متغير س: عدد = ٥

// ثابت غير قابل للتعديل
ثابت ط = ٣.١٤١٥٩
ثابت قائمة: مصفوفة<عدد> = [١، ٢، ٣]
```

### ٧.٢ المتغيرات العامة

المتغيرات المُعلَنة في المستوى الأعلى (خارج أي دالة) هي **متغيرات عامة**. يمكن الوصول إليها من جميع الدوال في الوحدة.

```tarqeem
// متغير عام قابل للتعديل
متغير عداد = ٠

// ثابت عام (يُدمَج وقت الترجمة)
ثابت الحد_الأقصى = ١٠٠

دالة زد() {
    عداد = عداد + ١  // الوصول للمتغير العام
}

دالة صفّر() {
    عداد = ٠  // تعديل المتغير العام
}
```

**السلوكيات الرئيسية**:
- الثوابت العامة ذات القيم المعروفة وقت الترجمة تُدمَج للتحسين
- المتغيرات العامة القابلة للتعديل تستخدم عمليات تحميل/تخزين وقت التشغيل
- المتغيرات المحلية في الدوال تُظلِّل المتغيرات العامة بنفس الاسم

### ٧.٣ جملة التعبير

```tarqeem
اطبع("مرحبا")؛        // استدعاء دالة
س = ٥؛                // إسناد
س++؛                  // زيادة
```

### ٧.٤ جملة الكتلة

```tarqeem
{
    متغير س = ٥
    اطبع(س)
}
```

### ٧.٥ جملة إذا

```tarqeem
// إذا بسيطة
إذا (شرط) {
    // كود
}

// إذا-وإلا
إذا (شرط) {
    // كود
} وإلا {
    // كود
}

// إذا-وإلا إذا-وإلا
إذا (شرط١) {
    // كود
} وإلا إذا (شرط٢) {
    // كود
} وإلا {
    // كود
}
```

### ٧.٦ حلقة طالما

```tarqeem
طالما (شرط) {
    // كود
}

// مع إيقاف
طالما (صحيح) {
    إذا (انتهى) {
        أوقف
    }
}

// مع استمرار
طالما (شرط) {
    إذا (تخطي) {
        استمر
    }
    // كود
}
```

### ٧.٧ حلقة لكل

```tarqeem
// حلقة لكل بنمط سي
لكل (متغير ع = ٠؛ ع < ١٠؛ ع++) {
    اطبع(ع)
}

// لكل-في (التكرار)
لكل عنصر في مجموعة {
    اطبع(عنصر)
}
```

### ٧.٨ حلقة افعل-طالما

```tarqeem
افعل {
    // كود - يُنفَّذ مرة واحدة على الأقل
} طالما (شرط)
```

### ٧.٩ جملة التطابق

```tarqeem
تطابق (قيمة) {
    حالة ١ => اطبع("واحد")
    حالة ٢، ٣ => اطبع("اثنان أو ثلاثة")
    حالة "نص" => {
        // جسم كتلي
        اطبع("نص")
    }
    غير_ذلك => اطبع("شيء آخر")
}
```

### ٧.١٠ جملة الإرجاع

```tarqeem
أرجع                  // إرجاع بدون قيمة
أرجع قيمة             // إرجاع قيمة
ارجع نتيجة            // تهجئة بديلة
```

### ٧.١١ أوقف واستمر

```tarqeem
أوقف                  // إيقاف الحلقة
اوقف                  // تهجئة بديلة
استمر                 // استمرار الحلقة
```

---

## ٨. الدوال

### ٨.١ إعلان الدالة

```tarqeem
// دالة بسيطة
دالة تحية() {
    اطبع("مرحبا")
}

// مع معاملات
دالة جمع(أ: عدد، ب: عدد) -> عدد {
    أرجع أ + ب
}

// مع معاملات افتراضية (مخطط)
دالة تحية(اسم: نص = "ضيف") {
    اطبع("مرحباً يا " + اسم)
}
```

### ٨.٢ نمط الإرجاع

يُحدَّد نمط الإرجاع بـ `->`:
```tarqeem
دالة مساحة(نصف_قطر: عدد_عشري) -> عدد_عشري {
    أرجع ٣.١٤١٥٩ * نصف_قطر * نصف_قطر
}
```

إذا لم يُحدَّد نمط الإرجاع، فالدالة لا تُرجع قيمة.

### ٨.٣ دوال لامدا

```tarqeem
// لامدا تعبيرية
ثابت مربع = (س: عدد) => س * س

// لامدا كتلية
ثابت معالج = (س: عدد) => {
    متغير نتيجة = س * ٢
    أرجع نتيجة
}

// أنماط مُستنتَجة
ثابت جمع = (أ، ب) => أ + ب
```

> **قيد حالي**: الدوال السهمية لا تلتقط (capture) متغيرات من نطاق دالة أو
> كتلة محيطة بها بعد — يمكنها الوصول فقط إلى معاملاتها الخاصة، متغيراتها
> المحلية، المتغيرات/الثوابت العامة على مستوى الوحدة، والدوال/الأصناف
> المُعرَّفة بالاسم. الإشارة إلى متغير خارجي تُنتج الخطأ [د٠٣٠٦](docs/رموز_الأخطاء/د/د٠٣٠٦.md).
> الالتقاطات الكاملة (closures) ميزة مخطط لها لاحقاً.

> **مثال عملي**: [`examples/دوال.ترقيم`](examples/دوال.ترقيم) يغطي
> كل صيغة مدعومة (لامدا تعبيرية وكتلية، تعليق نوع الدالة، الدوال العليا،
> دالة مُعلَنة كقيمة، دالة تُرجع دالة) مع توثيق القيود الحالية. يُنفَّذ في
> الأوضاع الثلاثة داخل تدفق التكامل المستمر، فمخرجاته متطابقة بينها.

### ٨.٤ استدعاء الدوال

```tarqeem
تحية()               // بدون معاملات
جمع(٥، ٣)            // مع معاملات
مصفوفة.طول()         // استدعاء دالة عضو
```

### ٨.٥ التكرار

```tarqeem
دالة فيبوناتشي(ن: عدد) -> عدد {
    إذا (ن <= ١) {
        أرجع ن
    }
    أرجع فيبوناتشي(ن - ١) + فيبوناتشي(ن - ٢)
}
```

### ٨.٦ الدوال المدمجة

| الدالة | الوصف |
|--------|-------|
| `اطبع(قيمة)` | الطباعة إلى المخرج القياسي |
| `طول(مصفوفة)` | الحصول على طول المجموعة |
| `بتات_و(أ، ب)` | الضرب المنطقي بين بتات عددين |
| `بتات_أو(أ، ب)` | الجمع المنطقي بين بتات عددين |
| `بتات_أو_حصري(أ، ب)` | الجمع المنطقي الحصري بين بتات عددين |
| `بتات_نفي(س)` | قلب بتات العدد جميعاً |
| `بتات_إزاحة_يسار(س، ن)` | دفع بتات العدد ن خانةً نحو الأعلى |
| `بتات_إزاحة_يمين(س، ن)` | دفع بتات العدد ن خانةً نحو الأدنى، مع تكرار خانة الإشارة |
| `بتات_إزاحة_يمين_منطقية(س، ن)` | دفع بتات العدد ن خانةً نحو الأدنى، مع إدخال أصفار من الأعلى |
| `حرف_إلى_رمز(س)` | رمز الحرف الأول من نص، و`-1` إن لم يكن له حرف أول |
| `رمز_إلى_حرف(ر)` | الحرف الذي رمزه ر، و`""` إن لم يكن الرمز حرفاً |
| `نص_إلى_ثنائي(س)` | بايتات النص في ترميز UTF-8، بايتاً في كل عنصر |
| `ثنائي_إلى_نص(ب)` | النص الذي ترميزه تلك البايتات، و`""` إن لم تكن ترميزاً سليماً |
| `قص_حروف(س، ب، ط)` | ط حرفاً من النص ابتداءً من الحرف ب، و`""` إن لم يكن هناك ما يُقتطع |
| `متغير_بيئة(اسم)` | قيمة متغيّر البيئة، و`""` إن لم يكن هناك ما يُرجَع |
| `أنهِ_البرنامج(حالة)` | إنهاء البرنامج بحالة الخروج `حالة & ٢٥٥`، بلا رسالة |
| `اكتب_مجرى(مجرى، بايتات)` | كتابة البايتات في المجرى، والجواب عددها، و`-١` إن لم يكن هناك ما يُكتب أو أين |
| `اقرأ_مجرى(مجرى، عدد)` | قراءة البايتات من المجرى حتى العدد أو نهايته، والجواب مصفوفة فارغة إن لم يكن هناك ما يُقرأ أو من أين |
| `حالة_مسار(مسار، حقل)` | ماذا في المسار: `حقل ٠` نوعه و`حقل ١` حجمه بالبايتات، و`-١` إن لم يكن هناك ما يُجاب |
| `احذف_مسار(مسار)` | حذف ما في المسار: ملفاً أو وصلةً أو مجلداً فارغاً، والجواب `خطأ` إن لم يكن هناك ما يُحذف |
| `معاملات_البرنامج()` | معاملات سطر الأوامر بلا اسم البرنامج، ومصفوفة فارغة إن لم يكن له معامل |
| `افتح_ملف(مسار، وضع)` | معرِّف الملف المفتوح، `٣` أو أكثر، و`-١` إن لم يكن هناك ما يُفتح أو كيف |

عمليات البتات دوالٌ لا عوامل: ترقيم لا تملك رموز `&` و`|` و`^` و`~` و`<<` و`>>`، وإضافتها تغييرٌ في الصياغة.
وبادئة `بتات_` لأن `و` و`أو` كلمتان مفتاحيتان، فلا يصح أن يكون أحدهما اسماً بمفرده
(أما ابتداء معرِّف بهما فمسموح: `وقت`، `أولوية`). و`بتات_نفي` غير `ليس` المنطقية:
تلك تنفي قيمة `منطقي`، وهذه تقلب بتات `عدد`.

```tarqeem
اطبع(بتات_و(12، 10))      // 8 — المشترك بين 1100 و1010
اطبع(بتات_و(4660، 255))   // 52 — البايت الأدنى
اطبع(بتات_أو(12، 10))     // 14 — مجموع بتات 1100 و1010
اطبع(بتات_أو_حصري(12، 10)) // 6 — المختلف بين 1100 و1010
اطبع(بتات_نفي(255))       // -256 — قلب البتات جميعاً
اطبع(بتات_إزاحة_يسار(3، 4)) // 48 — دفع 11 أربع خانات
اطبع(بتات_إزاحة_يمين(48، 4)) // 3 — وإعادتها
اطبع(بتات_إزاحة_يمين_منطقية(-1، 63)) // 1 — بلا إشارة تُكرَّر
```

`بتات_و` تفحص البتات و`بتات_أو` ترفعها، وبهما معاً يُستبدل حقلٌ مضغوط: تمحوه
الأولى ثم تكتبه الثانية.

```tarqeem
// استبدال البايت الأدنى من 0x1234 بـ 0xAB، فيصير 0x12AB أي 4779
اطبع(بتات_أو(بتات_و(4660، -256)، 171))
```

و`بتات_أو_حصري` تقلب البتات بدل أن تفحصها أو ترفعها، فهي وحدها بين الثنائية التي
تتراجع بنفسها: قلبُ القيمة بالقناع مرتين يعيدها كما كانت.

```tarqeem
// «حصري» وصفٌ لما تفعله — يُرفع البتّ في أحد الطرفين لا في كليهما — لا نقلاً لحروف XOR
اطبع(بتات_أو_حصري(بتات_أو_حصري(4660، 43981)، 43981))  // 4660
اطبع(بتات_أو_حصري(255، -1))                            // -256 — المتمم
```

المتمم هو ما تعجز عنه الأخريان: `-1` كل بتاته مرفوعة، فالقلب به يقلب البتات جميعاً،
وبه يُحسب معكوس القناع بدل كتابته عدداً سالباً.

و`بتات_نفي` تسميةٌ لهذا القلب لا قدرةٌ جديدة: `بتات_نفي(س)` و`بتات_أو_حصري(س، -1)`
يعطيان القيمة نفسها. الفرق أن الأولى تُغني قارئها عن معرفة أن `-1` كل بتاته مرفوعة.
وهي أحادية بخلاف الثلاث قبلها.

```tarqeem
اطبع(بتات_نفي(255))                    // -256 — معكوس قناع البايت الأدنى
اطبع(بتات_نفي(بتات_نفي(4660)) == 4660)  // صحيح — القلب مرتين يعيد الأصل
اطبع(بتات_أو(بتات_و(4660، بتات_نفي(255))، 171))  // 4779 — قناع محسوب لا مكتوب
```

`عدد` صحيح ذو إشارة بـ٦٤ بتّاً، فقلب بتاته هو `-س - ١` بالضبط: `بتات_نفي(0)` يساوي
`-1` و`بتات_نفي(42)` يساوي `-43`. وكذلك `بتات_و(-1، 255)` يساوي `255`،
و`بتات_أو(-1، 0)` يساوي `-1`، و`بتات_أو_حصري(-1، -1)` يساوي `0`.

أما `بتات_إزاحة_يسار` فأول ما يُغيّر مواضع البتات لا قيمها: الأربع قبلها تعالج كل بتّ
في خانته، وهذه تدفع البتات جميعاً نحو الأعلى وتملأ ما أخلته أصفاراً. وهي ضربٌ في قوى
الاثنين ما دام البتّ الأعلى لم يبلغ خانة الإشارة (٦٣).

```tarqeem
اطبع(بتات_إزاحة_يسار(1، 10))                  // 1024
اطبع(بتات_إزاحة_يسار(7، 5) == 7 * 2 ** 5)     // صحيح
اطبع(بتات_إزاحة_يسار(1، 63))                  // -9223372036854775808 — خانة الإشارة
```

وبها تُبنى الأقنعة بدل كتابتها أعداداً، فتكتمل العائلة: الإزاحة تصنع القناع، و`بتات_أو`
ترفع بتّه، و`بتات_و` مع `بتات_نفي` تخفضه.

```tarqeem
اطبع(بتات_إزاحة_يسار(1، 8) - 1)                          // 255 — قناع البايت الأدنى
اطبع(بتات_أو(4660، بتات_إزاحة_يسار(1، 2)))                // رفع البتّ الثاني
اطبع(بتات_و(4660، بتات_نفي(بتات_إزاحة_يسار(1، 2))))        // خفضه
```

**مقدار الإزاحة في كل إزاحة من `٠` إلى `٦٣`، وما خرج عن ذلك فإزاحةٌ كاملة: تخرج البتات
جميعاً من الكلمة، وتُملأ الخانات المُخلاة كما تُملأ في تلك الإزاحة دائماً.** وهذا هو
الجواب الحسابي لا قيمةً بديلة. والمقدار السالب خارج النطاق كغيره، وليس إزاحةً في الاتجاه
المقابل — لتلك اسمها. والعملية **كليّة**: لا تتوقف ولا ترمي، ولا تختلف فيها الخلفيات.

فـ`بتات_إزاحة_يسار` تملأ ما أخلته أصفاراً، فالناتج خارج النطاق `٠`:

```tarqeem
اطبع(بتات_إزاحة_يسار(1، 64))    // 0
اطبع(بتات_إزاحة_يسار(255، 1000)) // 0
اطبع(بتات_إزاحة_يسار(1، -1))    // 0
```

و`بتات_إزاحة_يمين` هي الاتجاه المعاكس، لا نظيراً مطابقاً: الأولى تُخلي الخانات الدنيا
فتملؤها أصفاراً، وهذه تُخلي الخانات العليا فتملؤها بخانة الإشارة — أي تكرِّر إشارة العدد.
فالسالب يبقى سالباً مهما أُزيح.

```tarqeem
اطبع(بتات_إزاحة_يمين(1024، 10))  // 1
اطبع(بتات_إزاحة_يمين(48، 4))     // 3
اطبع(بتات_إزاحة_يمين(-16، 4))    // -1
اطبع(بتات_إزاحة_يمين(-1، 63))    // -1 — الإشارة تُملأ ولا تُمحى
```

ولهذا فهي قسمةٌ على قوى الاثنين تُقرِّب نحو الأدنى، لا نحو الصفر كما يفعل عامل القسمة.
والفرق يظهر في السوالب وحدها، وهو ما يجعلها عمليةً مستقلة لا صيغةً أخرى لـ`/`.

```tarqeem
اطبع(بتات_إزاحة_يمين(7، 1) == 7 / 2)      // صحيح
اطبع(بتات_إزاحة_يمين(-7، 1))              // -4 — نحو الأدنى
اطبع(-7 / 2)                              // -3 — نحو الصفر
```

وتطبيق قاعدة النطاق عليها يعطي خانة الإشارة لا الصفر، لأن الإزاحة الكاملة هنا تملأ
الكلمة بها: `٠` للموجب و`-١` للسالب. ولو أُرجع `٠` لانقطع الحساب عند حدٍّ بلا سبب، إذ
`بتات_إزاحة_يمين(-1، 63)` هي `-1` أصلاً.

```tarqeem
اطبع(بتات_إزاحة_يمين(-1، 64))    // -1
اطبع(بتات_إزاحة_يمين(-1، 1000))  // -1
اطبع(بتات_إزاحة_يمين(-1، -1))    // -1
اطبع(بتات_إزاحة_يمين(255، 64))   // 0
```

وبالإزاحتين معاً تُقرأ الحقول المضغوطة وتُكتب: `بتات_إزاحة_يسار` تصنع القناع في موضعه،
و`بتات_و` تنتقي به، و`بتات_إزاحة_يمين` تُنزل ما انتُقي إلى قيمته.

```tarqeem
// 4660 = 0x1234، فبايته الأعلى 0x12 أي 18
اطبع(بتات_إزاحة_يمين(بتات_و(4660، بتات_إزاحة_يسار(255، 8))، 8))  // 18
اطبع(بتات_إزاحة_يمين(بتات_إزاحة_يسار(-3، 4)، 4))                 // -3 — ذهاب وإياب
```

أما `بتات_إزاحة_يمين_منطقية` فهي الإزاحة يميناً وقد أُسقطت منها الإشارة: تُخلي الخانات
العليا وتملؤها **أصفاراً** لا بخانة الإشارة. و`منطقية` هنا وصفٌ للإزاحة — أنها تعالج
البتات وحدها ولا تعرف إشارةً — لا صلة لها بنمط `منطقي`.

ولهذا لا تفترق عن `بتات_إزاحة_يمين` في الأعداد غير السالبة: لا إشارة مرفوعة تُكرَّر.
وإنما تفترق عنها في السوالب، وبعرض الكلمة كلها.

```tarqeem
اطبع(بتات_إزاحة_يمين_منطقية(48، 4))                                  // 3
اطبع(بتات_إزاحة_يمين_منطقية(48، 4) == بتات_إزاحة_يمين(48، 4))        // صحيح
اطبع(بتات_إزاحة_يمين(-1، 1))                                         // -1
اطبع(بتات_إزاحة_يمين_منطقية(-1، 1))                                  // 9223372036854775807
اطبع(بتات_إزاحة_يمين_منطقية(-16، 4))                                 // 1152921504606846975
اطبع(بتات_إزاحة_يمين_منطقية(-1، 63))                                 // 1 — بتّ الإشارة قيمةً
```

والإزاحة وحدها هي ما يُسقط الإشارة، فالمقدار `٠` لا يحرّك شيئاً ويُبقي السالب سالباً:

```tarqeem
اطبع(بتات_إزاحة_يمين_منطقية(-16، 0))  // -16
```

فإن كانت `بتات_إزاحة_يمين` قسمةً على قوى الاثنين تُقرِّب نحو الأدنى، فهذه قسمةُ العدد
**مقروءاً بلا إشارة** عليها. تطابق `/` في الموجب، وتفارقه في السالب:

```tarqeem
اطبع(بتات_إزاحة_يمين_منطقية(255، 4) == 255 / 2 ** 4)  // صحيح
اطبع(-1 / 2)                                          // 0
اطبع(بتات_إزاحة_يمين_منطقية(-1، 1))                   // 9223372036854775807
```

وقاعدة النطاق واحدة في الإزاحات الثلاث، والرقم يختلف بحسب ما تملؤه كلٌّ منها: هذه
تملأ أصفاراً، فالخارج عن النطاق `٠` ولو كان العدد سالباً — وهنا يعود الاتجاهان إلى
الاتفاق في القاعدة والاختلاف في الجواب.

```tarqeem
اطبع(بتات_إزاحة_يمين(-1، 64))            // -1 — الإشارة تُملأ
اطبع(بتات_إزاحة_يمين_منطقية(-1، 64))     // 0 — الأصفار تُملأ
اطبع(بتات_إزاحة_يمين_منطقية(-1، 1000))   // 0
اطبع(بتات_إزاحة_يمين_منطقية(-1، -1))     // 0
```

وبها يتمّ الذهاب والإياب فوق خانة الإشارة: القناع الذي تبنيه `بتات_إزاحة_يسار` في
الخانات العليا يجعل العدد سالباً، فلا تُعيده `بتات_إزاحة_يمين` — تُغرقه في الآحاد —
وتُعيده هذه كما كان.

```tarqeem
// 255 مدفوعاً إلى الخانات الثماني العليا، ثم مقروءاً منها
اطبع(بتات_إزاحة_يمين_منطقية(بتات_إزاحة_يسار(255، 56)، 56))  // 255
اطبع(بتات_إزاحة_يمين(بتات_إزاحة_يسار(255، 56)، 56))         // -1
```

أما `حرف_إلى_رمز` فأول مدمجة تعمل على النصوص لا على الأعداد، وهي كذلك أول ما ينزل إلى
**نداءٍ في مكتبة وقت التشغيل** بدل تعليمة في التمثيل الوسيط: قراءة رمز الحرف تقتضي فكّ
ترميز UTF-8، وليس لذلك تعليمة. وهي تُرجع رمز الحرف **الأول** وحده، ولا يعنيها ما بعده.

```tarqeem
اطبع(حرف_إلى_رمز("A"))      // 65
اطبع(حرف_إلى_رمز("م"))      // 1605
اطبع(حرف_إلى_رمز("مرحبا"))  // 1605 — الأول وحده
```

والمقروء **رمز** (codepoint) لا حرفٌ مرئي: الحركة رمزٌ مستقل يلي الحرف، فأول «مَرحبا»
هو الميم وحدها والفتحة رمزٌ ثانٍ. والمحلل اللغوي يطبّع الملف المصدري كلَّه تطبيعاً
معيارياً (NFC) قبل التجزئة، والنصوص الحرفية في ذلك كغيرها، فالجواب هو أول رمز **بعد
التطبيع**: نصٌّ كُتب فيه «أ» مفكوكاً (ا + همزة علوية) يُقرأ ١٥٧١ لا ١٥٧٥. أما النصوص
المبنية وقت التشغيل فلا يُعاد تطبيعها. والحركات في الأمثلة أدناه لا يغيّرها التطبيع، إذ
لا شكل مركّب للميم مع الفتحة.

```tarqeem
اطبع(حرف_إلى_رمز("مَرحبا"))  // 1605 — الميم
اطبع(حرف_إلى_رمز("َ"))       // 1614 — الفتحة رمزٌ بنفسها
```

وعرض الحرف في الترميز لا يظهر في الجواب: يشغل الحرف بايتاً واحداً أو اثنين أو ثلاثة أو
أربعة، والرمز واحدٌ في الجميع.

```tarqeem
اطبع(حرف_إلى_رمز("A"))   // 65 — بايت
اطبع(حرف_إلى_رمز("م"))   // 1605 — بايتان
اطبع(حرف_إلى_رمز("﷽"))   // 65021 — ثلاثة
اطبع(حرف_إلى_رمز("𞸀"))   // 126464 — أربعة
```

**و`-1` جوابها إن لم يكن للنص حرفٌ أول**، ولم يكن `٠` ليصلح لذلك: الرمز `٠` حرفٌ قائم
بنفسه (U+0000)، فلو كان هو الجواب لما تميّز النص الفارغ من نصٍّ أوله ذلك الحرف. ولهذا
تفارق `حرف_إلى_رمز` أختها `طول` التي تُرجع `٠` للفارغ، إذ `٠` هناك جوابٌ صحيح.
والنص الاختياري الفارغ (`لا_شيء`) كالنص الفارغ: بلا حرف أول.

```tarqeem
اطبع(حرف_إلى_رمز(""))     // -1
متغير غائب: نص? = لا_شيء
اطبع(حرف_إلى_رمز(غائب))    // -1
```

وبها تُقرأ الأرقام العربية، وهو ما لم يكن ممكناً قبلها: `عدد("٤٢")` يخفق اليوم لأن
التحويل يمرّ بمحلّل لا يعرف إلا الأرقام اللاتينية. والقراءة مقارنةٌ بمدى من الرموز،
والمدى لا يُقارَن به نص — و`حرف_في` تُرجع نصاً من حرف واحد.

```tarqeem
دالة قيمة_رقم_عربي(حرف: نص) -> عدد {
    ثابت رمز = حرف_إلى_رمز(حرف)
    إذا (رمز >= 1632 && رمز <= 1641) {   // ٠ إلى ٩
        أرجع رمز - 1632
    }
    أرجع -1
}
اطبع(قيمة_رقم_عربي("٧"))   // 7
اطبع(قيمة_رقم_عربي("م"))   // -1
```

و`رمز_إلى_حرف` عكسها: تلك تقرأ رمز الحرف، وهذه تبني الحرف من رمزه. وبها وحدها يُنشأ حرفٌ
محسوب، إذ `نص` تكتب العدد رقماً لا حرفاً — فـ`نص(٦٥)` هي `"65"` لا `"A"`.

```tarqeem
اطبع(رمز_إلى_حرف(65))      // A
اطبع(رمز_إلى_حرف(1605))    // م
اطبع(رمز_إلى_حرف(65021))   // ﷽
اطبع(رمز_إلى_حرف(126464))  // 𞸀
```

**و`""` جوابها إن لم يكن الرمز حرفاً**: أن يكون سالباً، أو نصفَ بديل (surrogate) من `55296` إلى
`57343`، أو فوق أعلى رمز في يونيكود (`1114111`). وأنصاف البدائل مرفوضة لأنها لا تقوم بنفسها —
إنما تُكتب مزدوجةً في ترميزٍ آخر — فلو بُنيت لأنتجت نصاً غير سليم.

```tarqeem
اطبع(طول(رمز_إلى_حرف(-1)))        // 0
اطبع(طول(رمز_إلى_حرف(55296)))     // 0 — نصف بديل
اطبع(طول(رمز_إلى_حرف(1114112)))   // 0 — فوق المدى
اطبع(طول(رمز_إلى_حرف(1114111)))   // 1 — أعلى رمز، وهو حرف
```

و`""` هنا تقابل `-1` هناك، فيتم الذهاب والإياب في الاتجاهين ولا تبقى ثغرة: `حرف_إلى_رمز("")`
هي `-1`، فكل رمزٍ مرفوض يعود `-1` بعينه.

```tarqeem
اطبع(رمز_إلى_حرف(حرف_إلى_رمز("م")))     // م
اطبع(حرف_إلى_رمز(رمز_إلى_حرف(126464)))  // 126464
اطبع(حرف_إلى_رمز(رمز_إلى_حرف(-1)))      // -1
```

والرمز `٠` حرفٌ قائم بنفسه لا نصٌّ فارغ، وهذا بعينه سبب اختيار `-1` سنتاً لـ`حرف_إلى_رمز`:

```tarqeem
اطبع(طول(رمز_إلى_حرف(0)))              // 1 — لا 0
اطبع(حرف_إلى_رمز(رمز_إلى_حرف(0)))       // 0
```

وبها تُكتب الأرقام العربية، فيكتمل ما بدأته أختها: تلك تقرأ «٧» عدداً، وهذه تكتب `٧` نصاً —
ولا تفعله `نص`، إذ `نص(٧)` هي `"7"` لاتينياً.

```tarqeem
دالة رقم_عربي_من_قيمة(قيمة: عدد) -> نص {
    إذا (قيمة >= 0 && قيمة <= 9) {
        أرجع رمز_إلى_حرف(1632 + قيمة)   // ٠ إلى ٩
    }
    أرجع ""
}
اطبع(رقم_عربي_من_قيمة(7))   // ٧
اطبع(رقم_عربي_من_قيمة(10))  // "" — ليس رقماً واحداً
```

أما `نص_إلى_ثنائي` فتنقل العمل من وحدة الرمز إلى وحدة **البايت**: تلك المدمجتان تقرأان الرمز
وتبنيانه، وهذه تُرجع أوكتيتات النص في ترميز UTF-8، بايتاً في كل عنصر. وهي أول مدمجة **أساسية**
تُرجع **مصفوفة** لا قيمةً مفردة — أما دوال المكتبة القياسية فمنها ما يُرجع مصفوفة قبلها.

```tarqeem
اطبع(نص_إلى_ثنائي("hi"))            // [104، 105]
اطبع(طول(نص_إلى_ثنائي("A")))        // 1
اطبع(طول(نص_إلى_ثنائي("م")))        // 2
اطبع(طول(نص_إلى_ثنائي("﷽")))        // 3
اطبع(طول(نص_إلى_ثنائي("𞸀")))        // 4
```

والفرق بين الوحدتين يظهر في `طول`: على النص تعدّ الأحرف، وعلى ناتج هذه تعدّ البايتات. والرقمان
كلاهما صحيح، ولكلٍّ وحدته — والحرف العربي بايتان، فهما يفترقان في كل نص غير لاتيني. وبهذه
المدمجة صار قياس البايتات ممكناً، وهو ما تُقاس به حدود التخزين والشبكة.

```tarqeem
اطبع(طول("مرحبا"))                  // 5 — أحرف
اطبع(طول(نص_إلى_ثنائي("مرحبا")))    // 10 — بايتات
```

**والمصفوفة الفارغة جوابها إن لم يكن للنص بايتات**، وهي قيمةٌ صحيحة لا سنت: النص الفارغ له
ترميزٌ واحد لا التباس فيه، فلا حاجة إلى ما يميّزه كما احتاجت `حرف_إلى_رمز` إلى `-1`. والنص
الاختياري الفارغ (`لا_شيء`) كالنص الفارغ.

```tarqeem
اطبع(طول(نص_إلى_ثنائي("")))   // 0
متغير غائب: نص? = لا_شيء
اطبع(طول(نص_إلى_ثنائي(غائب)))  // 0
```

والبايتات تُنقل كما هي بلا تحقق: النص في ترقيم سليم الترميز بحكم بنائه، ولا يُفحص هنا شيء.

والناتج مصفوفة كاملة الحقوق: تُقاس بـ`طول`، وتُفهرَس، وتُدار عليها حلقة `لكل … في`.

```tarqeem
متغير بايتات = نص_إلى_ثنائي("Az")
اطبع(بايتات[0])   // 65
اطبع(بايتات[1])   // 122

متغير مجموع = 0
لكل بايت في بايتات {
    مجموع = مجموع + بايت
}
اطبع(مجموع)       // 187
```

وبايتات النص اللاتيني هي رموز حروفه، فتعود حروفاً بـ`رمز_إلى_حرف`. أما الحرف العربي فبايتاته
ليست رمزه — «م» بايتاها `217` و`133` ورمزه `1605` — فإعادة تركيبه بـ`ثنائي_إلى_نص` أدناه.

```tarqeem
اطبع(رمز_إلى_حرف(نص_إلى_ثنائي("z")[0]))          // z
اطبع(حرف_إلى_رمز("z") == نص_إلى_ثنائي("z")[0])   // صحيح
```

أما `ثنائي_إلى_نص` فعكسها، وبها يتمّ الجسر: تلك تقرأ بايتات النص، وهذه تبني النص من بايتاته. ولم
تكن `رمز_إلى_حرف` تكفي لذلك، فهي تأخذ **رمز** الحرف لا بايتاته — وبايتا «م» ليسا رمزه.

```tarqeem
اطبع(ثنائي_إلى_نص([104، 105]))                    // hi
اطبع(ثنائي_إلى_نص([217، 133]))                    // م
اطبع(طول(ثنائي_إلى_نص([217، 133])))               // 1 — حرف واحد من بايتين
اطبع(ثنائي_إلى_نص(نص_إلى_ثنائي("مرحبا")))          // مرحبا
```

والدورة تامّة في الاتجاهين لكل نص: يخرج بايتاتٍ ويعود كما كان، ولا يقتصر ذلك على ناتج أختها —
تقبل هذه مصفوفةً مكتوبةً أو محسوبة، وبها تُقرأ الترميزات التي تُخرج بايتات لا نصوصاً.

```tarqeem
اطبع(ثنائي_إلى_نص(نص_إلى_ثنائي("𞸀")) == "𞸀")     // صحيح
اطبع(حرف_إلى_رمز(ثنائي_إلى_نص([217، 133])))       // 1605
```

**والمصفوفة يجب أن تكون ترميز UTF-8 لنصٍّ ما، وإلا فالجواب `""`.** وذلك في حالين: عنصرٌ خارج
`٠`-`٢٥٥` فليس بايتاً أصلاً، أو بايتاتٌ لا تُكوِّن ترميزاً سليماً — بايتٌ لا يصلح بادئةً، أو
تسلسلٌ مقطوع، أو ترميزٌ مُطوَّل، أو نصفُ بديل. والقيمة **لا تُقتطَع** إلى بايتها الأدنى: لو
اقتُطعت لأجاب `300` بـ`","` وهو ترميز `44`، فيلتبس المرفوض بالمقبول.

```tarqeem
اطبع(طول(ثنائي_إلى_نص([300])))          // 0 — ليس بايتاً
اطبع(طول(ثنائي_إلى_نص([-1])))           // 0
اطبع(طول(ثنائي_إلى_نص([255])))          // 0 — لا يصلح بادئةً
اطبع(طول(ثنائي_إلى_نص([217])))          // 0 — تسلسل مقطوع
اطبع(طول(ثنائي_إلى_نص([192، 128])))     // 0 — ترميز مُطوَّل
```

و**المصفوفة الفارغة جوابها `""` قيمةً لا رفضاً**، إذ لها ترميزٌ واحد لا التباس فيه — كما كانت
المصفوفة الفارغة جواب `نص_إلى_ثنائي("")`. فيبقى الرفض مميَّزاً بطول المُدخَل: ناتجٌ فارغ من
مصفوفةٍ غير فارغة رفضٌ لا غير.

```tarqeem
اطبع(طول(ثنائي_إلى_نص([])))             // 0 — قيمة صحيحة
متغير غائب: أي = لا_شيء
اطبع(طول(ثنائي_إلى_نص(غائب)))            // 0
```

> **لماذا تتحقّق وأختها لا تتحقّق؟** قراءة البايتات لا تخفق، وبناء النص يخفق. والنص في ترقيم
> سليم الترميز بحكم بنائه، فلو قُبل ما ليس ترميزاً لأنتج نصاً لا يستطيع المفسّر تمثيله أصلاً —
> ولطبعته الترجمة الأصلية فراغاً بلا خطأ. فالتحقّق هو ما يُبقي الخلفيات متفقة.

> **قيد حالي**: `نص` لا تُنسّق المصفوفات. فـ`نص(نص_إلى_ثنائي(س))` يرفضها المفسّر والترجمة
> الفورية بخطأ نوع، وتطبع الترجمة الأصلية **مؤشراً** عدداً عشرياً وتخرج بصفر
> ([المسألة ٣٣١](https://github.com/osama1998H/tarqeem/issues/331)). استخدم `طول` أو الفهرسة،
> وللطباعة `اطبع` مباشرة — فهي تعرف المصفوفات.

أما `قص_حروف` فأول مدمجة تُرجع **جزءاً** من نص لا نصاً كاملاً ولا عدداً، وهي وحدها في هذا الباب
تأخذ ثلاثة معاملات. تُرجع `ط` حرفاً ابتداءً من الحرف `ب`.

```tarqeem
اطبع(قص_حروف("مرحبا"، 1، 3))   // رحب
اطبع(قص_حروف("مرحبا"، 0، 5))   // مرحبا
```

و«حروف» في اسمها ليست حشواً، بل هي الفرق كلُّه: القصّ بالحرف لا بالبايت، فلا يُقطع الحرف العربي
نصفين. وذلك يقتضي المشي على ترميز UTF-8، إذ يشغل الحرف بايتاً أو بايتين أو ثلاثة أو أربعة —
والأعراض الأربعة تجتمع في نصٍّ واحد فيظهر الفرق:

```tarqeem
اطبع(قص_حروف("A﷽م𞸀ب"، 1، 3))   // ﷽م𞸀 — الثلاثة الوسطى كاملةً
اطبع(قص_حروف("𞸀م"، 0، 1))       // 𞸀 — أربعة بايتات، حرفٌ واحد
```

وبطلبِ حرفٍ واحد يُقرأ الحرف رقم كذا، وهو ما تعجز عنه `حرف_إلى_رمز` وحدها — تلك تقرأ الأول لا
غير. وبضمّ الاثنتين يُقرأ رمز أيّ حرف:

```tarqeem
اطبع(قص_حروف("مرحبا"، 2، 1))                 // ح
اطبع(حرف_إلى_رمز(قص_حروف("مرحبا"، 2، 1)))     // 1581
```

والوحدة هنا الحرف، والوحدة في `نص_إلى_ثنائي` البايت، فيفترق الرقمان في كل نص غير لاتيني وكلاهما
صحيح في وحدته:

```tarqeem
اطبع(طول(قص_حروف("مرحبا"، 1، 3)))                 // 3 — أحرف
اطبع(طول(نص_إلى_ثنائي(قص_حروف("مرحبا"، 1، 3))))   // 6 — بايتات
```

**والعملية كليّة، فلا تحتاج فحص مدى قبل النداء: بدايةٌ سالبة، أو بدايةٌ بعد آخر النص، أو طولٌ غير
موجب — الجواب `""` في الجميع. والطول الذي يتجاوز النهاية يُقصَر إلى ما بقي، لا يُرفض.**

```tarqeem
اطبع(طول(قص_حروف("مرحبا"، -1، 2)))   // 0
اطبع(طول(قص_حروف("مرحبا"، 9، 2)))    // 0
اطبع(طول(قص_حروف("مرحبا"، 1، 0)))    // 0
اطبع(طول(قص_حروف(""، 0، 1)))         // 0
اطبع(طول(قص_حروف("مرحبا"، 2، 99)))   // 3 — يُقصَر إلى ما بقي
```

و`""` هنا قيمةٌ لا سنت، بخلاف `-1` في `حرف_إلى_رمز`: النص الفارغ جوابٌ صحيح للقصّ الفارغ، ولا
التباس فيه — فمن أراد التمييز قاس ما أدخل.

وبها تُكتب دوال النصوص التي تمشي حرفاً حرفاً، ولم تكن تُكتب قبلها إلا بالبايتات فتُفسد العربية:

```tarqeem
دالة اعكس(س: نص) -> نص {
    متغير ناتج = ""
    متغير ي = طول(س) - 1
    طالما (ي >= 0) {
        ناتج = ناتج + قص_حروف(س، ي، 1)
        ي = ي - 1
    }
    أرجع ناتج
}
اطبع(اعكس("مرحبا"))     // ابحرم
اطبع(اعكس("A﷽م𞸀"))     // 𞸀م﷽A
```

أما `متغير_بيئة` فأول مدمجة تقرأ ما **خارج البرنامج** من إعداده: كل ما قبلها يحسب من مُدخَلاته،
وهذه تسأل نظام التشغيل عمّا سلّمه إلى العملية عند تشغيلها. ولذلك لا تُكتب بترقيم بحال — لا تركيبٌ من
المدمجات قبلها يبلغ البيئة، ولا صيغة في اللغة تصل إليها.

```tarqeem
اطبع(طول(متغير_بيئة("PATH")) > 0)   // صحيح — القيمة تختلف بين الأجهزة، وكونها غير فارغة لا يختلف
```

**و`""` جوابها إن لم يكن هناك ما يُرجَع**: أن يكون المتغيّر غائباً، أو الاسم فارغاً، أو المُدخَل
`لا_شيء`. وهي قيمةٌ لا سنت، بخلاف `-1` في `حرف_إلى_رمز`: للمتغيّر الغائب جوابٌ واحد لا التباس فيه،
فلم يُحتَج إلى ما يميّزه.

```tarqeem
اطبع(طول(متغير_بيئة("TARQEEM_ENV_ABSENT")))   // 0
اطبع(متغير_بيئة("TARQEEM_ENV_ABSENT") == "")  // صحيح
اطبع(طول(متغير_بيئة("")))                      // 0
متغير اسم_غائب: نص? = لا_شيء
اطبع(طول(متغير_بيئة(اسم_غائب)))                // 0
```

**والمضبوط الفارغ لا يُفارق الغائب**: كلاهما `""`، وهو عقد `getenv` نفسه. فمن أراد التمييز بينهما لم
يجد إليه سبيلاً، وذلك قصدٌ لا سهو. والاسم يُقرأ كما هو بلا حذف فراغات، فـ`" PATH "` ليس `"PATH"`.

```tarqeem
اطبع(طول(متغير_بيئة(" PATH ")))   // 0
```

وأكثر ما تُستخدم فيه إعدادٌ له قيمة افتراضية. وكون الجواب `""` لا `لا_شيء` يجعل الفحص قياساً للطول لا
مقارنةً بالغياب:

```tarqeem
دالة إعداد(اسم: نص، افتراضي: نص) -> نص {
    متغير قيمة = متغير_بيئة(اسم)
    إذا (طول(قيمة) == 0) {
        أرجع افتراضي
    }
    أرجع قيمة
}
اطبع(إعداد("TARQEEM_ENV_ABSENT"، "افتراضي"))   // افتراضي
```

أما `أنهِ_البرنامج` فأول مدمجة نتيجتها المرئية **حالة الخروج** لا قيمة: كل ما قبلها يُحسب أو
يُطبع أو يُقرأ، وهذه تُبلِّغ مَن شغّل البرنامج بما انتهى إليه. ولا يبلغها تركيبٌ من المدمجات
قبلها، ولا صيغةٌ في اللغة: `أرجع` تُنهي دالة لا برنامجاً، ونهاية الملف حالتها `٠` دائماً.

و`توقف` ليست بديلاً عنها ولا هي تخصيصٌ لها، بل عملٌ آخر: تلك حالتها `١` **دائماً** وتطبع
رسالتها في مجرى الخطأ، وهذه تُنهي بالحالة التي تُعطاها ولا تطبع شيئاً. فالأولى إبلاغٌ عن خطأ
فادح، والثانية إبلاغٌ بنتيجة — والنتيجة قد تكون نجاحاً.

```tarqeem
اطبع("قبل")
أنهِ_البرنامج(٣)
اطبع("لا يُطبع")   // ما بعد الإنهاء لا يُنفَّذ
```

**والحالة هي `حالة & ٢٥٥`**: عرض حالة الخروج في نظام التشغيل ثمانية بتات، و`عدد` أربعة
وستون، فلا بدّ للغة أن تُجيب عن الستة والخمسين الباقية. والقناع يُطبَّق في الخلفيات كلها لا
يُترك للنظام، وبذلك يكون الجواب واحداً على كل منصة — إذ النظم الشبيهة بيونكس تقتطع البايت
الأدنى، وويندوز يحفظ الكلمة كلها، فلو تُرك لهما لأجاب البرنامجُ الواحد بجوابين.

```tarqeem
أنهِ_البرنامج(٠)      // ٠ — النجاح
أنهِ_البرنامج(٢٥٥)    // 255 — أعلى الحالات
أنهِ_البرنامج(٢٥٦)    // 0 — بايتٌ كامل بعد النهاية يلتفّ
أنهِ_البرنامج(-١)     // 255
أنهِ_البرنامج(٣٠٠)    // 44
```

فالعملية **كليّة**: كل `عدد` حالةٌ صالحة، فلا تحتاج فحص مدى قبل النداء، ولا ترمي ولا تتوقف.
وهذا هو الجواب الحسابي لا قيمةً بديلة، على قاعدة الإزاحات نفسها.

**ولا تلتقطها `حاول`**: هي إنهاءٌ لا استثناء، فتمرّ بكل معالج حتى تنتهي العملية. ولو التقطتها
لاختلفت الخلفيات: تلتقطها المفسّرات وتُنهي الترجمة الأصلية.

```tarqeem
حاول {
    أنهِ_البرنامج(٣)
} التقط (خ) {
    اطبع("لا ينبغي طباعته")
}
```

وأكثر ما تُستخدم فيه إبلاغُ الفشل عن أداةٍ تُشغَّل من سطر الأوامر:

```tarqeem
دالة تحقق_من_الإعداد(مكتمل: منطقي) {
    إذا (ليس مكتمل) {
        اطبع_خطأ("الإعداد ناقص")
        أنهِ_البرنامج(١)
    }
}
```

> **الكسرة**: `أنهِ` أمرٌ حُذفت ياؤه، والكسرة تدلّ على الحذف. ويُقبل كذلك `أنه_البرنامج` بلا
> كسرة، كما تُقبل `ارم` مع `ارمِ` و`ارجع` مع `أرجع` — والاسمان يبلغان العملية نفسها.

أما `اكتب_مجرى` فأول مدمجة تُخرج **بايتات** لا قيمة: كل ما قبلها من الطباعة يأخذ قيمةً فيختار لها
صيغةً ويُلحق بها سطراً جديداً، وهذه تأخذ البايتات كما هي فتكتبها كما هي. وبها صار للغة مخرجٌ على
مستوى البايت، وهو ما لم يكن لها.

ولا تُبنى `اطبع` عليها ولا هي تُبنى على `اطبع`، وذلك قصدٌ لا نقص: تلك مدمجةٌ في المترجم لأن اختيار
صيغة الطباعة يقع على النوع الساكن، وذلك الاختيار هو التوزيع نفسه — ولا تُكتب في ترقيم دالةٌ تأخذ
`أي` وتُترجَم ترجمةً أصلية. فهذه هي الكتابة المجرَّدة التي تُبنى عليها المكتبة القياسية وما يأتي من
عمليات الملفات، لا بديلٌ عن الطباعة.

```tarqeem
اكتب_مجرى(١، نص_إلى_ثنائي("مرحبا\n"))   // مرحبا — ويُرجع ١١
اطبع(طول("مرحبا"))                        // 5 — أحرف
```

والرقمان كلاهما صحيح ولكلٍّ وحدته: `طول` تعدّ الأحرف، والجواب هنا عدد **البايتات** المكتوبة —
والحرف العربي بايتان، فهما يفترقان في كل نص غير لاتيني.

و**المجرى** عددٌ يسمّي الموضع: `١` المخرج القياسي، و`٢` مجرى الخطأ، و`٣` وما فوقها معرِّف ملف. ولا
شيء في اللغة يفتح ملفاً بعد، فكل معرِّف من الثالثة فصاعداً لا يسمّي موضعاً قائماً — والجواب `-١`.

**و`-١` جوابها إن لم يكن هناك أين يُكتب أو ما يُكتب.** والمجرى يُفحص قبل البايتات، فالكتابة إلى غير
موضع مرفوضةٌ مهما كانت تحمل. و`٠` مجرى الدخل فالكتابة فيه خطأ لا مرادفٌ للمخرج، والمعرِّف السالب لا
يسمّي شيئاً.

```tarqeem
اطبع(اكتب_مجرى(٠، نص_إلى_ثنائي("س")))    // -1 — مجرى الدخل
اطبع(اكتب_مجرى(٣، نص_إلى_ثنائي("س")))    // -1 — لا معرِّف بهذا الرقم
اطبع(اكتب_مجرى(-١، نص_إلى_ثنائي("س")))   // -1
```

و`-١` تصلح جواباً للرفض لأن عدد البايتات لا يكون سالباً، فلا يلتبس المرفوض بالمقبول. وهي كذلك جواب
الكتابة التي تخفق — كأن يكون المجرى قد أُغلق — فالجواب عددُ ما بلغ المجرى، لا عددُ ما طُلب.

**والعنصر خارج `٠`-`٢٥٥` ليس بايتاً، فتُرفض الكتابة كلها ولا يُقتطع إلى بايته الأدنى.** ولو اقتُطع
لأجاب `٣٠٠` كما يُجيب `٤٤` — وهو الفاصلة — فلا يتميّز المرفوض من المقبول؛ وهو عين ما رُفض في
`ثنائي_إلى_نص`. والرفض **تامّ**: يُفحص المصفوف كله قبل أن يخرج أول بايت، فالبايت السليم الذي قبل
الفاسد لا يُكتب.

```tarqeem
اطبع(اكتب_مجرى(١، [٣٠٠]))       // -1
اطبع(اكتب_مجرى(١، [-١]))        // -1
اطبع(اكتب_مجرى(١، [٦٥، ٣٠٠]))   // -1 — ولا يُكتب A
```

وهذا الفحص هو نفسه ما يحمي من مصفوفةٍ ليست بايتات أصلاً: عناصر `مصفوفة<نص>` مؤشّرات، وقيمة المؤشّر
أكبر من بايت بكثير، فتُرفض بدل أن تُكتب عناويناً.

**والمصفوفة الفارغة جوابها `٠` قيمةً لا سنتاً**: لا شيء يُكتب فلا شيء كُتب، و`لا_شيء` كذلك — كلاهما
لا يفقد شيئاً بأن يشتركا في جواب، إذ معناهما واحد.

```tarqeem
اطبع(اكتب_مجرى(١، []))                  // 0
اطبع(اكتب_مجرى(١، نص_إلى_ثنائي("")))    // 0
```

**ولا تُلحق سطراً جديداً من عندها**، وهذا فرقها الظاهر عن `اطبع`: تلك تُنهي سطرها، وهذه لا تكتب إلا
ما أُعطيت. فبكتبتين متتاليتين يُبنى سطرٌ واحد، وهو ما لا تفعله الطباعة.

```tarqeem
اكتب_مجرى(١، نص_إلى_ثنائي("الاسم: "))
اكتب_مجرى(١، نص_إلى_ثنائي("ترقيم\n"))   // الاسم: ترقيم
```

وأكثر ما تُستخدم فيه كتابةٌ لا تُنهي سطرها: طلبُ إدخال، أو مؤشّر تقدّم، أو حقلٌ يُتبَع بغيره.

وهي كذلك تكتب ما ليس نصاً: البايتات تُنقل كما هي بلا فحص ترميز، فتصلح لمخرجٍ ثنائي — بخلاف الطباعة،
التي تفكّ الترميز أولاً فلا تُخرج ما لا يُفكّ.


أما `اقرأ_مجرى` فالنصف الآخر من الزوج: تلك تُخرج البايتات وهذه تُدخلها. وهي أول ما يقرأ **بايتاً** —
كل ما قبلها من الإدخال يقرأ سطراً كاملاً ويُرجعه نصاً مفكوك الترميز، فلا يبلغ به البرنامجُ بايتاً
واحداً، ولا يقرأ قدراً معلوماً، ولا يُقطِّع الأسطر بنفسه. وبها انتقل التقطيع من مكتبة وقت التشغيل إلى
اللغة.

```tarqeem
متغير بايتات = اقرأ_مجرى(٠، ٣٢)   // حتى ٣٢ بايتاً من مجرى الدخل
اطبع(طول(بايتات))                  // 10 — لـ«مرحبا» في الدخل
اطبع(ثنائي_إلى_نص(بايتات))         // مرحبا
```

والرقمان كلاهما صحيح ولكلٍّ وحدته، كما في أختها: `طول` على الناتج تعدّ **البايتات**، وعلى النص تعدّ
الأحرف — والحرف العربي بايتان.

و**المجرى** عددٌ يسمّي الموضع كما في `اكتب_مجرى`، غير أن الأدوار تنعكس: `٠` مجرى الدخل، و`٣` وما
فوقها معرِّف ملف. أما `١` و`٢` فتحملان البايتات في الاتجاه المعاكس، فالقراءة منهما لا تُرجع شيئاً.
ولا شيء في اللغة يفتح ملفاً بعد، فكل معرِّف من الثالثة فصاعداً لا يسمّي موضعاً قائماً.

**والقراءة تُعيد المحاولة حتى تبلغ العدد المطلوب أو ينتهي المجرى.** ولو قرأت مرةً واحدة لأجابت بما
صادفته في الأنبوب، فاختلف الجواب بين تشغيلٍ وتشغيل وبين خلفيةٍ وخلفية — وذلك اضطرابٌ لا خطأ، أعنفُ
ما يُصاب به الاختبار. وبالتكرار يصير للجواب القصير معنًى واحد: انتهى المجرى. وهو عين ما اختارته
`اكتب_مجرى` في الاتجاه المقابل حين كتبت البايتات كلها ولم تُبلِّغ عن كتابةٍ ناقصة.

```tarqeem
// «abc» في الدخل، والمطلوب مئة
اطبع(طول(اقرأ_مجرى(٠، ١٠٠)))   // 3 — ما كان هناك
اطبع(طول(اقرأ_مجرى(٠، ١٠٠)))   // 0 — ولم يبقَ شيء
```

والنداءات المتتابعة تُكمل من حيث انتهى ما قبلها، فالمجرى يُستهلَك ولا يُعاد.

**والمصفوفة الفارغة جواب كل ما ليس بايتات**: نهاية المجرى، ومجرىً لا يُقرأ منه، ومعرِّفٌ لا يسمّي
شيئاً، وعددٌ غير موجب. **ولا يتميّز أحدها من الآخر، وذلك قصدٌ لا سهو**: المصفوفة لا تملك قيمةً فاضلة
تصلح سنتاً كما تملك `اكتب_مجرى` عددَها السالب — إذ كل مصفوفة جوابٌ مشروع — فمن أراد التمييز نظر في
المجرى الذي طلبه. وهذا هو عرف المكتبة نفسه: قراءة السطر تُرجع `""` للنهاية وللمعرِّف المجهول سواء.

```tarqeem
اطبع(طول(اقرأ_مجرى(١، ٤)))     // 0 — مجرى إخراج
اطبع(طول(اقرأ_مجرى(٢، ٤)))     // 0
اطبع(طول(اقرأ_مجرى(٣، ٤)))     // 0 — لا معرِّف بهذا الرقم
اطبع(طول(اقرأ_مجرى(-١، ٤)))    // 0
```

**والعدد غير الموجب لا يُقرأ به شيء، ولا يُمَسّ المجرى أصلاً**، فما فيه باقٍ لما بعده:

```tarqeem
// «abc» في الدخل
اطبع(طول(اقرأ_مجرى(٠، ٠)))          // 0
اطبع(طول(اقرأ_مجرى(٠، -٥)))         // 0
اطبع(ثنائي_إلى_نص(اقرأ_مجرى(٠، ٣)))  // abc — لم يُستهلَك شيء
```

فالعملية **كليّة**: كل زوج `(عدد، عدد)` نداءٌ صالح، فلا تحتاج فحص مدى قبله، ولا ترمي ولا تتوقف.

**وكونها بايتاتٍ لا نصاً هو المقصود**: الحرف الذي تقطعه قراءتان يبقى سليماً بين قطعتيه، ويقع الفكّ
مرةً واحدة بعد أن تكتمل البايتات. فـ«مرحبا» ترميزها `D9 85 D8 B1 …`، وثلاثة بايتات تقطع «ر» نصفين —
ومع ذلك لا يسقط منها شيء:

```tarqeem
متغير أول = اقرأ_مجرى(٠، ٣)
متغير بقية = اقرأ_مجرى(٠، ٧)
اطبع(أول[٢])                    // 216 — نصف «ر» الأول
اطبع(بقية[٠])                   // 177 — ونصفه الثاني، متّصلاً
اطبع(طول(ثنائي_إلى_نص(أول)))     // 0 — القطعة وحدها ليست ترميزاً سليماً
```

ولو كان الإدخال نصاً لتعذَّر ذلك: النص لا يحمل نصفَ حرف.

وهي كذلك تقرأ ما ليس نصاً — البايتات تُنقل كما هي بلا فكّ ترميز، فيبلغ البرنامجَ بايتٌ لا يقوم حرفاً:

```tarqeem
// البايتات ٢٥٥ و٠ و٦٥ في الدخل
متغير خام = اقرأ_مجرى(٠، ٣)
اطبع(خام[٠])                    // 255
اطبع(طول(ثنائي_إلى_نص(خام)))     // 0 — ليس ترميزاً، فالفكّ يرفض
```


أما `حالة_مسار` فأول مدمجة تسأل **نظام الملفات**: ما قبلها يحسب من مُدخَلاته، أو يقرأ مجرىً ويكتب
فيه، وهذه تسأل عمّا في مسارٍ لم يُفتَح. ولا يبلغها تركيبٌ من المدمجات قبلها، ولا صيغةٌ في اللغة —
فليس في ترقيم ما يعرف أموجودٌ ملفٌ أم لا.

وهي `stat(2)`: **حقلٌ واحد في كل نداء**، فيبقى الجواب `عدد` ولا تعبر بنيةٌ حدودَ اللغة. وعبورُ
البنية هو ما أعطب دوال التاريخ التسع، فلا يُعاد.

```tarqeem
اطبع(حالة_مسار("."، ٠))          // 2 — مجلد
اطبع(حالة_مسار("."، ١))          // -1 — والمجلد لا حجم له
```

**والنوع أربعة**: `٠` غير موجود، و`١` ملف، و`٢` مجلد، و`٣` موجود وليس أحدهما — كالأجهزة والمقابس
والأنابيب المسمّاة. والرابع ليس زيادةً: به وحده تُجمَع الأسماء الأربعة في اسم، إذ `ملف_موجود` تُجيب
عن الجهاز بـ`صحيح` و`هل_ملف` تُجيب عنه بـ`خطأ`، فلو كانت الأنواع ثلاثة لتعذّر أن يُجيب حقلٌ واحد
عن الاثنين.

```tarqeem
اطبع(حالة_مسار("/dev/null"، ٠))   // 3 — موجود، وليس ملفاً ولا مجلداً
```

**والحجم صفةُ الملف وحده**: `-١` للمعدوم، و`-١` للمجلد، و`-١` لما ليس ملفاً. أما `حجم_ملف` فتُجيب
اليوم عن المجلد بما يعطيه النظام — ٤٠٩٦ على لينكس و٦٤ على ماك — وذاك رقمٌ يتغيّر بنظام الملفات، فلا
يصلح في عقدٍ ولا يُثبَّت في اختبار. فالجواب هنا واحدٌ على كل منصة، وهو فرقٌ مقصود عن تلك، لا سهو.

**والحقل يُفحص قبل المسار**: سؤالٌ بلا حقل لا جواب له مهما كان في المسار، فلا يُمَسّ نظام الملفات
لحقلٍ مجهول. وهو ترتيب `اكتب_مجرى` نفسه، إذ تفحص المجرى قبل البايتات.

```tarqeem
اطبع(حالة_مسار("."، ٢))    // -1
اطبع(حالة_مسار("."، ٩))    // -1
اطبع(حالة_مسار("."، -١))   // -1
```

فالعملية **كليّة**: كل زوج `(نص، عدد)` نداءٌ صالح، فلا تحتاج فحص مدى قبله، ولا ترمي ولا تتوقف.

**والوصلات الرمزية تُتبَع**، كما تتبعها الأسماء الأربعة التي تُجمَع فيها، فالجواب عن الوصلة هو
الجواب عمّا تسمّيه. ولذلك تُقرأ الوصلة المقطوعة **معدومةً**: الوصلة موجودة، ولا شيء في المسار الذي
تسمّيه — والسؤال عن المسار لا عن الوصلة.

**والمعدوم، وما لا يُقرأ لمنع، والاسم الفارغ، و`لا_شيء` — جوابها واحد لا يتميّز، وذلك قصدٌ لا
سهو.** والمسار يُقرأ كما هو بلا حذف فراغات، فـ`" تقرير.نص "` ليس `"تقرير.نص"` — إذ الفراغ حرفٌ في
الاسم كغيره.

```tarqeem
اطبع(حالة_مسار("لا_يوجد_هذا_المسار"، ٠))   // 0
اطبع(حالة_مسار(""، ٠))                     // 0
متغير غائب: نص? = لا_شيء
اطبع(حالة_مسار(غائب، ٠))                   // 0
```

ومن أراد التمييز نظر في المسار الذي أعطى، وهو عين ما تفعله `متغير_بيئة` في المضبوط الفارغ والغائب،
و`اقرأ_مجرى` في نهاية المجرى والمجرى الذي لا يُقرأ منه.

وأكثر ما تُستخدم فيه أن تُكتب أغلفةً بأسماء الأسئلة، فتقرأ الشروط قراءةً طبيعية:

```tarqeem
دالة هل_موجود(مسار: نص) -> منطقي {
    أرجع حالة_مسار(مسار، ٠) != ٠
}

دالة هل_ملف(مسار: نص) -> منطقي {
    أرجع حالة_مسار(مسار، ٠) == ١
}

دالة هل_مجلد(مسار: نص) -> منطقي {
    أرجع حالة_مسار(مسار، ٠) == ٢
}

دالة حجم(مسار: نص) -> عدد {
    أرجع حالة_مسار(مسار، ١)
}

اطبع(هل_موجود("."))   // صحيح
اطبع(هل_مجلد("."))    // صحيح
اطبع(هل_ملف("."))     // خطأ
```

ولاحظ أن `هل_موجود` **تنفي `٠`** ولا تُثبت `١`، وذلك هو النوع الرابع عاملاً: الجهاز موجودٌ وليس
ملفاً، فمن أثبت `١` أخطأ فيه.

> **ملاحظة على الاسم**: `مسار` لا `ملف`، لأن المسؤول عنه مسارٌ قد يكون فيه ملفٌ أو مجلدٌ أو غيرهما،
> والمجلد ليس ملفاً. وهي الكلمة التي تستعملها أختها `احذف_مسار` للمدى نفسه.


أما `احذف_مسار` فأخت التي قبلها: تلك **تسأل** نظام الملفات، وهذه **تعمل** فيه. وهي أول مدمجة أساسية
أثرُها في نظام الملفات لا رجعة فيه — و`اكتب_مجرى` تكتب، لكنها لا تبلغ إلا المخرج القياسي ومجرى
الخطأ، إذ لا شيء في اللغة يفتح ملفاً بعد.

وهي `unlink(2)` للملف و`rmdir(2)` للمجلد الفارغ، وتُجمَع فيها `احذف_ملف` و`احذف_مجلد`.

```tarqeem
اطبع(احذف_مسار("تقرير.نص"))         // صحيح — والملف يزول
اطبع(احذف_مسار("مجلد_فارغ"))        // صحيح — والمجلد يزول
اطبع(احذف_مسار("."))                // خطأ — المجلد غير فارغ
```

**والمُنتقي بين العمليتين `lstat` لا `stat`**، وهذا فرقٌ مقصود عن أختها لا سهو: تلك تتبع الوصلات
الرمزية فتُجيب عن **الهدف**، وهذه لا تتبعها فتعمل على **الاسم**. فالوصلة تُحذف بنفسها ولا تُرمَد،
ويبقى ما تسمّيه.

```tarqeem
// وصلةٌ إلى مجلد: أختها تراها مجلداً، وهذه تراها وصلةً
اطبع(حالة_مسار("وصلة_إلى_مجلد"، ٠))   // 2 — الهدف مجلد
اطبع(احذف_مسار("وصلة_إلى_مجلد"))      // صحيح — الوصلة تزول
اطبع(حالة_مسار("المجلد_الهدف"، ٠))    // 2 — والهدف باقٍ
```

ولو اتُّبعت الوصلة لصار الجواب `خطأ` هنا، إذ `rmdir` ترفض الوصلة — وهي مقبولةٌ في `احذف_ملف` اليوم.
**والأهمّ أن الوصلة المقطوعة تُقرأ معدومةً في `حالة_مسار`**، فما بُني على `stat` لا يحذفها أبداً بل
يتركها معلَّقةً إلى الأبد. وهذه تحذفها:

```tarqeem
اطبع(حالة_مسار("وصلة_مقطوعة"، ٠))   // 0 — لا شيء في المسار الذي تسمّيه
اطبع(احذف_مسار("وصلة_مقطوعة"))      // صحيح — والوصلة نفسها موجودة، فتُحذف
```

**وهي غير متعدية**: المجلد غير الفارغ مرفوض، فيبقى عقد `احذف_مجلد` كما هو. والحذف المتعدي حلقةٌ في
المكتبة القياسية لا مدمجةٌ ثانية، كما أن إنشاء المجلدات المتداخلة حلقةٌ لا مدمجة.

**و`خطأ` جواب كل ما ليس هناك ما يُحذف**: المعدوم، والاسم الفارغ، و`لا_شيء`، وما لا يُقرأ لمنع، والمجلد
العامر. **ولا يتميّز أحدها من الآخر**، وذلك قصدٌ لا سهو — كما لا يتميّز الغائب من المضبوط الفارغ في
`متغير_بيئة`، ولا نهايةُ المجرى من المجرى الذي لا يُقرأ منه في `اقرأ_مجرى`. ومن أراد التمييز سأل
`حالة_مسار` قبل الحذف. والمسار يُقرأ كما هو بلا حذف فراغات.

```tarqeem
اطبع(احذف_مسار("لا_يوجد_هذا_المسار"))   // خطأ
اطبع(احذف_مسار(""))                     // خطأ
متغير غائب: نص? = لا_شيء
اطبع(احذف_مسار(غائب))                   // خطأ
```

فالعملية **كليّة**: كل `نص` نداءٌ صالح، فلا تحتاج فحص وجودٍ قبله، ولا ترمي ولا تتوقف.

وأكثر ما تُستخدم فيه أن تُكتب غلافَين بأسماء العمليتين المجموعتين فيها:

```tarqeem
دالة احذف_ملف(مسار: نص) -> منطقي {
    إذا (حالة_مسار(مسار، ٠) == ٢) {
        أرجع خطأ
    }
    أرجع احذف_مسار(مسار)
}

دالة احذف_مجلد(مسار: نص) -> منطقي {
    إذا (حالة_مسار(مسار، ٠) != ٢) {
        أرجع خطأ
    }
    أرجع احذف_مسار(مسار)
}
```

> **فرقٌ موثَّق في الغلافين**: النوع الذي يسألان عنه يأتي من `حالة_مسار` التي **تتبع** الوصلات،
> والحذف **لا يتبعها**. فالوصلة إلى مجلدٍ يرفضها `احذف_ملف` هنا وهي مقبولةٌ فيه اليوم، ويقبلها
> `احذف_مجلد` هنا وهي مرفوضةٌ فيه اليوم. حافةٌ واحدة بوجهين، ولا يبلغها الغلافان معاً إلا بنوعٍ
> لا يتبع الوصلات، وليس في اللغة اليوم من يُجيب به.


أما `معاملات_البرنامج` فأول مدمجة تقرأ ما **سُئل عنه** البرنامج: ما قبلها يحسب من مُدخَلاته، أو يقرأ
مجرىً، أو يسأل نظام الملفات، أو يقرأ ما حول البرنامج من بيئته — وهذه تقرأ ما وُجِّه إليه هو. وبها
تكتمل أداةُ سطر الأوامر التي بدأتها `أنهِ_البرنامج`: تلك تُبلِّغ مَن شغّل البرنامج بما انتهى إليه،
وهذه تقرأ ما طلبه منه. ولا يبلغها تركيبٌ ممّا قبلها: المعاملات متجهٌ غير البيئة، فلا تصل إليها
`متغير_بيئة`، ولا صيغة في اللغة تفتح ما يحملها.

```tarqeem
// tarqeem run برنامج.ترقيم أول ثان
متغير م = معاملات_البرنامج()
اطبع(طول(م))   // 2
اطبع(م[٠])     // أول
```

**واسم البرنامج ليس منها**، وذلك قصدٌ لا سهو: اسمه في الترجمة الأصلية مسارُ الملف التنفيذي، وفي
المفسّر مسارُ الملف المصدري، فلو عُدَّ منها لاختلف الجواب بين الخلفيتين أبداً. وبإخراجه يصير جواب
البرنامج الذي لا معامل له مصفوفةً فارغة في الخلفيات الثلاث سواء. والاسم يوافق ذلك: «معاملات
البرنامج» ما يُعطاه، لا ما يُسمَّى به.

```tarqeem
// ./مخرج أول — والمصفوفة عنصر واحد لا اثنان
اطبع(طول(معاملات_البرنامج()))   // 1
```

**والمصفوفة الفارغة جوابها إن لم يكن له معامل**، وهي قيمةٌ صحيحة لا سنت — كما كانت جواب
`نص_إلى_ثنائي("")` — إذ «لا معاملَ له» معنًى واحد لا التباس فيه. وهي كذلك جوابه إن لم يُشغَّل من
مسار التشغيل المعتاد، كالتنقيح؛ والحالان لا يتميّزان، كما لا يتميّز في `متغير_بيئة` الغائبُ من
المضبوط الفارغ.

```tarqeem
اطبع(طول(معاملات_البرنامج()))   // 0 — بلا معاملات
اطبع(معاملات_البرنامج())        // []
```

والمعاملات تُنقل كما هي: لا تُحذف فراغاتها، ولا يُعاد تقسيمها — فالصَّدَفة قسمتها قبل أن تبلغ
البرنامج — ولا يسقط منها الفارغ. وما ليس ترميزاً سليماً يُقرأ قراءةً متساهلة، فيحلّ محلّ كل تسلسل
فاسد الرمزُ `U+FFFD`، ولا يُحذف المعامل ولا يتغيّر موضعه.

```tarqeem
// tarqeem run برنامج.ترقيم "ثان ثالث" ""
اطبع(طول(معاملات_البرنامج()))      // 2
اطبع(معاملات_البرنامج()[٠])         // ثان ثالث
اطبع(طول(معاملات_البرنامج()[١]))    // 0 — الفارغ باقٍ في موضعه
```

**والمعامل الذي يشبه الخيار معاملٌ للبرنامج لا للمترجم**: كلّ ما بعد اسم الملف يخصّ البرنامج.

```tarqeem
// tarqeem run برنامج.ترقيم -س --jit
اطبع(معاملات_البرنامج()[٠])   // -س
اطبع(معاملات_البرنامج()[١])   // --jit — نصٌّ للبرنامج، لا خيارٌ للمترجم
```

> **فرقٌ موثَّق في الشرطتين**: `--` في أول المعاملات تبتلعها أداةُ سطر الأوامر علامةَ فصلٍ لها،
> كما تفعل `cargo run --`، فـ`tarqeem run ب.ترقيم -- أ` جوابها `["أ"]` بينما `./مخرج -- أ` جوابها
> `["--"، "أ"]` — إذ الملف التنفيذي لا محلّل أمامه. والفرق **محصور ومَهروب منه**: تكرارها
> (`-- -- أ`) يعطي جواب الترجمة الأصلية بعينه، و`--` في أيّ موضع بعد الأول تُنقل كما هي في
> الخلفيات الثلاث سواء.

**والنداء المتكرر يُجيب بما أجاب أولَ مرة**: المعاملات حالةُ عمليةٍ تُقرأ ولا تُستهلَك، بخلاف
`اقرأ_مجرى` التي يَنقُص مجراها بقراءته.

```tarqeem
اطبع(طول(معاملات_البرنامج()) == طول(معاملات_البرنامج()))   // صحيح
```

فالعملية **كليّة**: لا فحص قبلها، ولا ترمي ولا تتوقف.

وأكثر ما تُستخدم فيه قراءةُ معاملٍ له قيمة افتراضية، وبها و`أنهِ_البرنامج` تُكتب الأداة كاملةً:

```tarqeem
دالة معامل(موضع: عدد، افتراضي: نص) -> نص {
    متغير م = معاملات_البرنامج()
    إذا (موضع < طول(م)) {
        أرجع م[موضع]
    }
    أرجع افتراضي
}

دالة رئيسية() {
    متغير الملف = معامل(٠، "")
    إذا (طول(الملف) == ٠) {
        اطبع_خطأ("الاستعمال: أداة <ملف>")
        أنهِ_البرنامج(١)
    }
    اطبع("المطلوب: " + الملف)
}
```

> **قيد حالي**: `اطبع` على مصفوفة نصوص **عامرة** تطبع في الترجمة الأصلية عناوين عناصرها لا قيمها
> ([المسألة ٣٥٩](https://github.com/osama1998H/tarqeem/issues/359)) — و`trq_print_array` تقرأ كل
> عنصر عدداً صحيحاً. والمصفوفة الفارغة سليمة، إذ لا تدور حلقة العناصر أصلاً. فاطبع العناصر واحداً
> واحداً بالفهرسة أو بحلقة `لكل … في` حتى تُصلَح.


أما `افتح_ملف` فأول مدمجة تُنشئ **معرِّفاً** يبقى بعد نداءها: كل ما قبلها يحسب أو يطبع أو يسأل، وما
يعرفه زوج المجاري من المواضع ثلاثة لا تُفتَح — المخرج القياسي، ومجرى الخطأ، ومجرى الدخل. وبها صار
للبرنامج أن يسمّي ملفاً فيقرأه بالبايت أو يكتب فيه، وهو ما لم يكن ممكناً: `اقرأ_ملف` تقرأ الملف كله
نصاً واحداً، فلا تبلغ به بايتاً ولا قدراً معلوماً.

وهي تجمع الفواتح الثلاث في مكتبة وقت التشغيل خلف **وضع** واحد، فلا تُضاف ثلاثة أسماء لعملية واحدة:
`٠` قراءة، و`١` كتابة تُنشئ الملف أو تقتطعه، و`٢` إلحاق تُنشئه ولا تمحو ما فيه.

```tarqeem
متغير كاتب = افتح_ملف("تقرير.نص"، ١)
اطبع(اكتب_مجرى(كاتب، نص_إلى_ثنائي("مرحبا")))   // 10 — بايتات لا أحرف
```

**والمعرِّف `٣` أو أكثر أبداً**، لأن `٠` و`١` و`٢` محفوظة للمجاري القياسية؛ فيمضي الجواب إلى
`اكتب_مجرى` أو `اقرأ_مجرى` بلا مزاحمة. والنداءان المتتاليان معرِّفان مختلفان، ولا يُعاد معرِّف بعد
انصرافه.

وفي برنامجٍ آخر، أو على ملفٍ موجود من قبل، تُقرأ البايتات نفسها — أما القراءة في **البرنامج نفسه**
بعد الكتابة فلها قيدٌ في آخر هذا القسم:

```tarqeem
// «تقرير.نص» موجود قبل تشغيل هذا البرنامج
متغير قارئ = افتح_ملف("تقرير.نص"، ٠)
اطبع(ثنائي_إلى_نص(اقرأ_مجرى(قارئ، ٦٤)))   // مرحبا
```

**والجواب `-١` عند الفشل لا `٠`**، وهذا مُلزَم لا مُختار: `٠` هو مجرى الدخل، فلو أجاب الفشلُ به لمضى
المعرِّف الفاسد إلى `اقرأ_مجرى(٠، ن)` فقرأ لوحة المفاتيح **ونجح** — وذلك أسوأ من خطأ، إذ لا يُرى.
و`-١` مرفوضة في المجريين أصلاً، وهي جواب `اكتب_مجرى` عند فشلها.

**والوضع يُفحص قبل المسار**، كما تفحص `حالة_مسار` حقلها قبل مسارها و`اكتب_مجرى` مجراها قبل بايتاتها:
وضعٌ مجهول ليس طلباً يُشغَل به نظام الملفات، فلا يُنشئ شيئاً.

```tarqeem
اطبع(افتح_ملف("جديد.نص"، ٩))        // -1
اطبع(حالة_مسار("جديد.نص"، ٠))       // 0 — ولم يُنشأ
```

**والأوضاع ثلاثة، و`٣` مرفوض**: يُعلن `stdlib/ملفات/ملف.ترقيم` ثابتاً باسم `وضع_قراءة_كتابة` قيمته
`٣`، ولا اتجاه في هذه المدمجة يخدمه — فالمعرِّف قارئٌ أو كاتب، لا الأمران — فيُرفض مع بقية الأوضاع
المجهولة بدل أن يُخدَم بأحد نصفيه صامتاً.

**و`-١` جواب كل ما ليس هناك ما يُفتح**: مسارٌ معدوم في وضع القراءة، أو مسارٌ لا يُقرأ لمنع، أو اسمٌ
فارغ، أو `لا_شيء`. ولا يتميّز أحدها من الآخر، وذلك قصدٌ لا سهو — كما لا يتميّز الغائب من المضبوط
الفارغ في `متغير_بيئة`. والمسار يُقرأ كما هو بلا حذف فراغات.

```tarqeem
اطبع(افتح_ملف("لا_يوجد_هذا"، ٠))   // -1
اطبع(افتح_ملف(""، ٠))              // -1
متغير غائب: نص? = لا_شيء
اطبع(افتح_ملف(غائب، ٠))            // -1
```

فالعملية **كليّة**: كل زوج `(نص، عدد)` نداءٌ صالح، فلا تحتاج فحصاً قبلها، ولا ترمي ولا تتوقف.

**والمعرِّف يحمل اتجاهه، وزوج المجاري يحترمه**: الكتابة في معرِّف قراءة تُجيب `-١`، وقراءة معرِّف
كتابة تُجيب مصفوفة فارغة — وهما جوابا المعرِّف الذي لم يُفتَح أصلاً، فلا يُفرَّق بين الاتجاه الخاطئ
والمعرِّف المجهول.

**والمجلد يُفتح للقراءة ولا يُقرأ منه شيء**، وهو نقلٌ أمين لـ`open(2)` و`read(2)`: معرِّف المجلد
مقروءٌ في حساب النظام، والقراءة منه تخفق. أما الكتابة والإلحاق فيرفضان المجلد.

```tarqeem
متغير م = افتح_ملف("."، ٠)
اطبع(م >= ٣)                    // صحيح
اطبع(طول(اقرأ_مجرى(م، ٦٤)))     // 0
اطبع(افتح_ملف("."، ١))          // -1
```

> **قيد حالي**: ما يُكتب في معرِّف لا يبلغ الملف **يقيناً** قبل نهاية البرنامج. فـ`اكتب_مجرى` لا
> تُفرِغ المخزن في مسار المعرِّف قصداً — المخزن للتجميع — ولا شيء في اللغة يُغلق معرِّفاً بعد، فلا
> سبيل إلى الإفراغ. والكتابات كلها تُفرَّغ عند انتهاء البرنامج انتهاءً طبيعياً أو بـ`أنهِ_البرنامج`،
> فما كُتب يبلغ الملف. أما قراءة الملف نفسه في البرنامج نفسه قبل ذلك فقد تُجيب بأقل مما كُتب:
>
> ```tarqeem
> متغير كاتب = افتح_ملف("ملف.نص"، ١)
> اطبع(اكتب_مجرى(كاتب، نص_إلى_ثنائي("مرحبا")))   // 10
> متغير قارئ = افتح_ملف("ملف.نص"، ٠)
> اطبع(طول(اقرأ_مجرى(قارئ، ٦٤)))                  // 0 — لم يُفرَّغ بعد
> ```
>
> والخلفيات الثلاث متفقة في ذلك، فليس اختلافاً بينها. و`اغلق_ملف` هي التي تُعجّل الإفراغ.

### ٨.٧ نقاط دخول البرنامج

تدعم ترقيم وضعين تنفيذيين متعارضين:

#### وضع السكربت

في وضع السكربت، الجمل التنفيذية في المستوى الأعلى تُعرِّف نقطة دخول البرنامج. يلف المترجم هذا الكود تلقائياً في دالة رئيسية.

```tarqeem
بسم_الله

// المتغيرات العامة (مسموحة)
متغير عداد = ٠

// كود تنفيذي في المستوى الأعلى - هذه نقطة الدخول
اطبع("مرحباً بالعالم!")
عداد = عداد + ١
اطبع("العداد: " + عداد)

الحمد_لله
```

#### وضع البرنامج

في وضع البرنامج، `دالة رئيسية()` تُعرِّف نقطة الدخول صراحةً.

```tarqeem
بسم_الله

// المتغيرات العامة (مسموحة)
متغير الاسم: نص = "ترقيم"
ثابت الإصدار = "١.٠.٠"

// دالة مساعدة
دالة تحية(اسم: نص) {
    اطبع("مرحباً يا " + اسم + "!")
}

// نقطة الدخول الرئيسية
دالة رئيسية() {
    اطبع("=== وضع البرنامج ===")
    تحية(الاسم)
    اطبع("=== انتهى البرنامج ===")
}

الحمد_لله
```

#### تعارض الوضعين (خطأ ترجمة)

**مهم**: لا يمكن للبرنامج استخدام كلا الوضعين في آن واحد. إذا وُجِدت جمل تنفيذية في المستوى الأعلى و`دالة رئيسية()` في نفس الملف، يُنتَج خطأ ترجمة:

```tarqeem
// ❌ خطأ: لا يمكن وجود كلا الوضعين
بسم_الله

اطبع("كود المستوى الأعلى")    // نقطة دخول وضع السكربت

دالة رئيسية() {                // نقطة دخول وضع البرنامج
    اطبع("في الرئيسية")        // هذا لن يُنفَّذ أبداً!
}

الحمد_لله
```

**خطأ [ت٠٢٠١]**: لا يمكن وجود جمل تنفيذية عليا ودالة رئيسية() في نفس الملف.

#### مبررات التصميم

يضمن هذا التصميم:

١. **سلوك متوقع**: نقطة دخول واحدة فقط - لا غموض حول ما يُنفَّذ أولاً
٢. **صديق للأدوات**: بيئات التطوير والمنقحات يمكنها تحديد نقطة الدخول بسهولة
٣. **القابلية للتوسع**: وضع البرنامج يشجع التنظيم المهيكل للكود في المشاريع الكبيرة
٤. **البساطة**: وضع السكربت يسمح بسكربتات سريعة بدون نمطية

#### ما يُعَد كوداً تنفيذياً في المستوى الأعلى

| نوع الجملة | وضع السكربت؟ | مسموح مع `دالة رئيسية()`؟ |
|------------|--------------|---------------------------|
| `متغير` / `ثابت` (إعلانات عامة) | لا | نعم ✓ |
| `دالة` (إعلانات دوال) | لا | نعم ✓ |
| `صنف` (إعلانات أصناف) | لا | نعم ✓ |
| `ميثاق` (إعلانات مواثيق) | لا | نعم ✓ |
| `اطبع()` (استدعاءات دوال) | نعم ✗ | لا - يسبب خطأ |
| `إذا` / `طالما` (التحكم في التدفق) | نعم ✗ | لا - يسبب خطأ |
| التعبيرات والإسنادات | نعم ✗ | لا - يسبب خطأ |

---

## ٩. البرمجة الكائنية

### ٩.١ إعلان الصنف

```tarqeem
صنف شخص {
    // الحقول
    خاص اسم: نص
    خاص عمر: عدد

    // المنشئ
    منشئ(اسم: نص، عمر: عدد) {
        هذا.اسم = اسم
        هذا.عمر = عمر
    }

    // الدوال
    عام دالة تحية() {
        اطبع("مرحباً، أنا " + هذا.اسم)
    }
}
```

### ٩.٢ محددات الرؤية

| المحدد | الوصول |
|--------|--------|
| `عام` | يمكن الوصول من كل مكان |
| `خاص` | يمكن الوصول فقط داخل الصنف |
| `محمي` | يمكن الوصول في الصنف والأصناف الفرعية |

الرؤية الافتراضية هي `عام`.

### ٩.٣ المنشئات

```tarqeem
صنف شخص {
    خاص اسم: نص

    منشئ(اسم: نص) {
        هذا.اسم = اسم
    }
}
```

### ٩.٤ مرجع النسخة

استخدم `هذا` للإشارة إلى النسخة الحالية:
```tarqeem
هذا.اسم              // الوصول للحقل
هذا.دالة()           // استدعاء الدالة
```

### ٩.٥ الأعضاء المشتركة

```tarqeem
صنف رياضيات {
    مشترك ط: عدد_عشري = ٣.١٤١٥٩

    مشترك دالة جذر(س: عدد_عشري) -> عدد_عشري {
        // التنفيذ
    }
}

// الاستخدام
اطبع(رياضيات.ط)
متغير نتيجة = رياضيات.جذر(١٦.٠)
```

### ٩.٦ الوراثة

```tarqeem
صنف موظف يرث شخص {
    خاص راتب: عدد_عشري

    منشئ(اسم: نص، عمر: عدد، راتب: عدد_عشري) {
        الأصل(اسم، عمر)      // استدعاء منشئ الأب
        هذا.راتب = راتب
    }

    عام دالة تحية() {      // تجاوز دالة الأب
        الأصل.تحية()        // استدعاء دالة الأب
        اطبع("أنا موظف")
    }
}
```

### ٩.٧ المواثيق

```tarqeem
ميثاق قابل_للطباعة {
    دالة اطبع_معلومات()
}

ميثاق قابل_للمقارنة {
    دالة قارن(آخر: أي) -> عدد
}

صنف شخص يلتزم قابل_للطباعة {
    عام دالة اطبع_معلومات() {
        // التنفيذ مطلوب
    }
}
```

### ٩.٨ الالتزام بمواثيق متعددة

```tarqeem
صنف منتج يلتزم قابل_للطباعة، قابل_للمقارنة {
    // يجب تنفيذ جميع دوال المواثيق
}
```

### ٩.٩ الأنماط المعممة

```tarqeem
صنف قائمة<ن> {
    خاص عناصر: مصفوفة<ن>

    منشئ() {
        هذا.عناصر = []
    }

    عام دالة أضف(عنصر: ن) {
        هذا.عناصر.ألحق(عنصر)
    }

    عام دالة احصل(فهرس: عدد) -> ن {
        أرجع هذا.عناصر[فهرس]
    }
}

// الاستخدام
متغير أرقام = جديد قائمة<عدد>()
أرقام.أضف(١)
أرقام.أضف(٢)
```

### ٩.١٠ إنشاء الكائنات

```tarqeem
متغير شخص = جديد شخص("أحمد"، ٣٠)
متغير موظف = جديد موظف("محمد"، ٣٥، ١٠٠٠٠.٠)
```

---

## ١٠. الوحدات والاستيراد

### ١٠.١ جملة الاستيراد

```tarqeem
// استيراد مسمى
استورد { قائمة، قاموس } من "مجموعات"

// استيراد شامل مع اسم مستعار
استورد * كـ رياضيات من "رياضيات"

// استيراد افتراضي
استورد مساعد من "./مساعدات"
```

### ١٠.٢ جملة التصدير

```tarqeem
// تصدير دالة
صدّر دالة مساعدة() { }

// تصدير صنف
صدّر صنف أداة { }

// تصدير متغير
صدّر ثابت الإصدار = "١.٠.٠"

// إعادة تصدير كل ما تصدّره وحدة أخرى
صدّر * من "./اساسي"

// إعادة تصدير أسماء محددة
صدّر { باني_نص، باني_جديد } من "./بناء"
```

**إعادة التصدير** هي الصيغة التي تُبنى بها وحدات الفهرس في المكتبة القياسية:
يجمع ملف `فهرس.ترقيم` صادرات وحداته الفرعية في واجهة واحدة. لاحظ أن
`صدّر *` تتطلب `من "مسار"` — لا يوجد شكل مجرَّد يعيد تصدير الوحدة الحالية.

> **قيد حالي**: الاستيراد الشامل يتطلب اسماً مستعاراً (`استورد * كـ س من "…"`)؛
> لا يوجد استيراد شامل يُدخل الأسماء إلى النطاق الحالي بلا فضاء اسم. استخدم
> `صدّر * من "…"` لإعادة التصدير، أو استيراداً مسمى للاستهلاك.

### ١٠.٣ استبيان الوحدات

- المسارات النسبية: `"./ملف"`، `"../مجلد/ملف"`
- المكتبة القياسية: `"مجموعات"`، `"رياضيات"`، `"ملفات"`
- وحدات الحزم: `"حزمة/وحدة"`

---

## ١١. معالجة الاستثناءات

### ١١.١ كائنات الاستثناء

تستخدم ترقيم نموذج معالجة استثناءات مهيكل. **فقط كائنات الاستثناء يمكن رميها** - النصوص والأعداد والأنماط الأولية الأخرى لا يمكن رميها مباشرة.

> **ملاحظة:** الكلمة `خطأ` محجوزة للقيمة المنطقية. صنف الاستثناء الأساسي اسمه `استثناء`.

#### صنف الاستثناء الأساسي

صنف `استثناء` **معرَّف تلقائياً في كل برنامج** — لا يُعرَّف ولا يُستورَد. وهو الأصل الذي تقيس عليه `ارمِ` ما يصح رميه:

```tarqeem
صنف استثناء {
    عام رسالة: نص              // سبب الاستثناء، يُقرأ في «التقط»

    منشئ(رسالة: نص) {
        هذا.رسالة = رسالة
    }
}
```

محاولة تعريف صنف بهذا الاسم تُنتج الخطأ [ص٠٦٠٢](docs/رموز_الأخطاء/ص/ص٠٦٠٢.md)؛ الطريقة المدعومة لإضافة نوع استثناء هي التوريث.

> **قيد حالي**: هذا الصنف كان يذكر هنا منشئين — أحدهما يأخذ `رسالة_عربية` — لكن ترقيم لا تدعم تعدد المنشئات بعد (`ClassInfo.constructor` واحد). المتاح فعلياً هو المنشئ ذو المعامل الواحد، وحقل `رسالة_عربية` غير موجود.

#### أنماط الاستثناء القياسية

> **غير متوفرة بعد**: الأصناف المتخصصة أدناه ليست معرَّفة في المكتبة القياسية حالياً. عرّفها بالتوريث كما في القسم التالي حتى تُضاف.

| الاستثناء | الوصف |
|-----------|-------|
| `استثناء_قيمة` | قيمة غير صالحة |
| `استثناء_نوع` | عدم تطابق الأنماط |
| `استثناء_فهرس` | فهرس خارج الحدود |
| `استثناء_ملف` | خطأ في عملية الملف |
| `استثناء_شبكة` | خطأ شبكة |
| `استثناء_قسمة` | قسمة على صفر |

#### أصناف الاستثناء المخصصة

أنشئ أنماط استثناء مخصصة بتوريث `استثناء`:

```tarqeem
صنف استثناء_تحقق يرث استثناء {
    عام الحقل: نص

    منشئ(الحقل: نص، رسالة: نص) {
        الأصل(رسالة)
        هذا.الحقل = الحقل
    }
}
```

### ١١.٢ حاول-التقط-أخيراً

```tarqeem
حاول {
    // كود قد يرمي استثناء
    متغير نتيجة = عملية_خطرة()
} التقط (خ) {
    // معالجة الاستثناء - 'خ' نمطه صنف الاستثناء الأساسي
    اطبع("حدث استثناء: " + خ.رسالة)
} أخيراً {
    // يُنفَّذ في المسار الطبيعي وبعد «التقط» — راجع القيد في §١١.٤
    تنظيف()
}
```

**ملاحظة:** معامل الالتقاط نمطه تلقائياً `استثناء`، مما يوفر الوصول لخاصية `.رسالة` وحقول الاستثناء الأخرى.

### ١١.٣ جملة الرمي

جملة `ارمِ` **تتطلب كائن استثناء**:

```tarqeem
دالة قسمة(أ: عدد، ب: عدد) -> عدد {
    إذا (ب == ٠) {
        // صحيح: رمي كائن استثناء
        ارمِ جديد استثناء_قسمة("لا يمكن القسمة على صفر")
    }
    أرجع أ / ب
}

// باستخدام دالة مساعدة
دالة استثناء_جديد(رسالة: نص) -> استثناء {
    أرجع جديد استثناء(رسالة)
}

دالة ف() {
    ارمِ استثناء_جديد("حدث استثناء")
}
```

**رميات غير صالحة (أخطاء وقت الترجمة):**
```tarqeem
// هذه ستسبب الخطأ ص٠٦٠١:
ارمِ "نص"           // ❌ لا يمكن رمي نص
ارمِ ٤٢              // ❌ لا يمكن رمي عدد
ارمِ صحيح           // ❌ لا يمكن رمي منطقي
ارمِ جديد شخص()    // ❌ لا يمكن رمي صنف غير استثناء
```

> **قيد حالي في الترجمة الأصلية**: `ارمِ` مدعومة في المفسّر (`tarqeem run`) وفي
> الترجمة الفورية (`tarqeem run --jit`)، ولم تُنفَّذ بعد في مولّد الكود الأصلي —
> فمولّد الكود لا يملك آلية فكّ (unwinding) تنقل التحكّم إلى `التقط`. ترجمة
> برنامج يحتوي `ارمِ` بـ `tarqeem compile` تُنتج الخطأ
> [ت٠٣٠٣](docs/رموز_الأخطاء/ت/ت٠٣٠٣.md). كتلة `حاول`/`التقط`/`أخيراً` بلا رمي
> تُترجَم وتعمل في الأوضاع الثلاثة.

### ١١.٤ انتشار الاستثناء

تنتشر الاستثناءات صعوداً في مكدس الاستدعاء حتى تُلتَقط:

```tarqeem
دالة أ() {
    ب()  // الاستثناء من ب() ينتشر إذا لم يُلتَقط
}

دالة ب() {
    ارمِ جديد استثناء("استثناء من ب")
}

حاول {
    أ()
} التقط (خ) {
    اطبع(خ.رسالة)  // يلتقط الاستثناء من ب()
}
```

استثناء لا يجد معالجاً في أي إطار يُنهي البرنامج برمز خروج غير صفري، مع طباعة
`رسالة` الاستثناء.

> **قيد حالي**: كتلة `أخيراً` تُنفَّذ في المسار الطبيعي وبعد `التقط` فقط. لا
> تُنفَّذ عند أي خروج غير طبيعي من كتلة `حاول`:
> - انتشار استثناء خارج الدالة
> - استثناء يلتقطه `حاول` خارجي في نفس الدالة
> - `أرجع` مبكرة من داخل `حاول`
> - `أوقف` أو `استمر` من داخل `حاول` داخل حلقة
>
> السبب أن التمثيل الوسيط لا يملك تعليمة «أعد الرمي» تسمح بتشغيل كتلة التنظيف
> ثم متابعة الانتشار. لا تضع في `أخيراً` تحريراً لمورد يعتمد عليه البرنامج في
> هذه المسارات حتى تُعالَج.

---

## ١٢. التزامن

### ١٢.١ الدوال المتوازية

```tarqeem
متوازي دالة احضر_بيانات(رابط: نص) -> نص {
    متغير استجابة = انتظر طلب_شبكة(رابط)
    أرجع استجابة.نص()
}
```

### ١٢.٢ تعبير الانتظار

```tarqeem
متوازي دالة رئيسية() {
    متغير بيانات = انتظر احضر_بيانات("https://api.example.com")
    اطبع(بيانات)
}
```

### ١٢.٣ نموذج التنفيذ المتوازي

- الدوال المتوازية تُرجع كائناً شبيهاً بالوعد
- `انتظر` يوقف التنفيذ حتى يُحَل الوعد
- وقت التشغيل يدير حلقة أحداث لعمليات الإدخال/الإخراج

---

## ١٣. نموذج الذاكرة

### ١٣.١ نظرة عامة

تستخدم ترقيم نهجاً هجيناً لإدارة الذاكرة:
- **تخصيص المكدس**: الأوليات والقيم ثابتة الحجم الصغيرة
- **تخصيص الكومة**: الكائنات، المصفوفات، النصوص
- **عد المراجع**: إدارة ذاكرة تلقائية لكائنات الكومة

### ١٣.٢ أنماط القيمة مقابل أنماط المرجع

**أنماط القيمة** (تُمرَّر بالنسخ):
- `عدد`
- `عدد_عشري`
- `منطقي`

**أنماط المرجع** (تُمرَّر بالمرجع):
- `نص`
- `مصفوفة`
- نسخ `صنف`
- `قاموس`

### ١٣.٣ عد المراجع

تُحرَّر الكائنات تلقائياً عندما لا تبقى مراجع:
```tarqeem
{
    متغير قائمة = [١، ٢، ٣]  // عدد المراجع = ١
    متغير أخرى = قائمة       // عدد المراجع = ٢
}  // كلا المتغيرين يخرجان من النطاق، العدد = ٠، تُحرَّر
```

### ١٣.٤ أمان القيمة الفارغة

الأنماط الاختيارية يجب فحصها قبل الاستخدام:
```tarqeem
متغير س: عدد? = لا_شيء

// الاستخدام المباشر سيكون خطأ
// اطبع(س + ١)  // خطأ!

// الاستخدام الآمن
إذا (س != لا_شيء) {
    اطبع(س + ١)  // مقبول، س معروف أنه ليس فارغاً
}
```

---

## ١٤. المكتبة القياسية

### ١٤.١ الأنماط الأساسية

| الوحدة | المحتويات |
|--------|-----------|
| `مجموعات` | `قائمة<ن>`، `قاموس<م،ق>`، `مجموعة<ن>` |
| `رياضيات` | `جذر()`، `قوة()`، `مطلق()`، دوال مثلثية |
| `نص` | أدوات معالجة النصوص |
| `ملفات` | عمليات نظام الملفات |
| `شبكة` | الشبكات (HTTP، مآخذ) |

### ١٤.٢ الدوال المدمجة

| الدالة | الوصف |
|--------|-------|
| `اطبع()` | الإخراج إلى المخرج القياسي |
| `طول()` | الحصول على طول المجموعة |
| `نوع()` | الحصول على اسم النمط |
| `بتات_و()` | الضرب المنطقي بين بتات عددين |
| `بتات_أو()` | الجمع المنطقي بين بتات عددين |
| `بتات_أو_حصري()` | الجمع المنطقي الحصري بين بتات عددين |
| `بتات_نفي()` | قلب بتات العدد جميعاً |
| `بتات_إزاحة_يسار()` | دفع بتات العدد ن خانةً نحو الأعلى |
| `بتات_إزاحة_يمين()` | دفع بتات العدد ن خانةً نحو الأدنى، مع تكرار خانة الإشارة |
| `بتات_إزاحة_يمين_منطقية()` | دفع بتات العدد ن خانةً نحو الأدنى، مع إدخال أصفار من الأعلى |
| `حرف_إلى_رمز()` | رمز الحرف الأول من نص، و`-1` إن لم يكن له حرف أول |
| `رمز_إلى_حرف()` | الحرف الذي رمزه ر، و`""` إن لم يكن الرمز حرفاً |
| `نص_إلى_ثنائي()` | بايتات النص في ترميز UTF-8، بايتاً في كل عنصر |
| `ثنائي_إلى_نص()` | النص الذي ترميزه تلك البايتات، و`""` إن لم تكن ترميزاً سليماً |
| `قص_حروف()` | ط حرفاً من النص ابتداءً من الحرف ب، و`""` إن لم يكن هناك ما يُقتطع |
| `متغير_بيئة()` | قيمة متغيّر البيئة، و`""` إن لم يكن هناك ما يُرجَع |
| `أنهِ_البرنامج()` | إنهاء البرنامج بحالة الخروج `حالة & ٢٥٥`، بلا رسالة |
| `اكتب_مجرى()` | كتابة البايتات في المجرى، والجواب عددها، و`-١` إن لم يكن هناك ما يُكتب أو أين |
| `اقرأ_مجرى()` | قراءة البايتات من المجرى حتى العدد أو نهايته، والجواب مصفوفة فارغة إن لم يكن هناك ما يُقرأ أو من أين |
| `حالة_مسار()` | ماذا في المسار: `حقل ٠` نوعه و`حقل ١` حجمه بالبايتات، و`-١` إن لم يكن هناك ما يُجاب |
| `احذف_مسار()` | حذف ما في المسار: ملفاً أو وصلةً أو مجلداً فارغاً، والجواب `خطأ` إن لم يكن هناك ما يُحذف |
| `معاملات_البرنامج()` | معاملات سطر الأوامر بلا اسم البرنامج، ومصفوفة فارغة إن لم يكن له معامل |
| `افتح_ملف()` | معرِّف الملف المفتوح، `٣` أو أكثر، و`-١` إن لم يكن هناك ما يُفتح أو كيف |

### ١٤.٣ دوال المصفوفة

| الدالة | الوصف |
|--------|-------|
| `ألحق()` | إضافة للنهاية |
| `احذف_آخر()` | حذف من النهاية |
| `طول` | الحصول على الطول |
| `عيّن()` | تحويل العناصر |
| `رشّح()` | ترشيح العناصر |

---

## ١٥. القواعد النحوية الرسمية

### ١٥.١ الترميز

```
<غير_طرفي>
'حرفي'
[اختياري]
{صفر-أو-أكثر}
(تجميع)
|   بديل
```

### ١٥.٢ بنية البرنامج

```
برنامج           := {جملة}

جملة             := إعلان_متغير
                  | إعلان_دالة
                  | إعلان_صنف
                  | إعلان_ميثاق
                  | جملة_إذا
                  | جملة_طالما
                  | جملة_لكل
                  | جملة_تطابق
                  | جملة_إرجاع
                  | جملة_أوقف
                  | جملة_استمر
                  | جملة_حاول
                  | جملة_ارم
                  | جملة_استورد
                  | جملة_صدر
                  | جملة_تعبير
                  | كتلة
```

### ١٥.٣ الإعلانات

```
إعلان_متغير      := ('متغير' | 'ثابت') معرِّف [':' نمط] ['=' تعبير] '؛'

إعلان_دالة       := ['متوازي'] 'دالة' معرِّف '(' [معاملات] ')' ['->' نمط] كتلة

إعلان_صنف        := 'صنف' معرِّف ['<' معاملات_نمط '>']
                    ['يرث' معرِّف]
                    ['يلتزم' معرِّف {'،' معرِّف}]
                    '{' {عضو_صنف} '}'

إعلان_ميثاق      := 'ميثاق' معرِّف ['<' معاملات_نمط '>']
                    '{' {توقيع_دالة} '}'

جملة_استورد      := 'استورد' عناصر_استيراد 'من' نص '؛'

عناصر_استيراد    := '{' معرِّف ['كـ' معرِّف] {'،' معرِّف ['كـ' معرِّف]} '}'
                  | '*' 'كـ' معرِّف
                  | معرِّف                       // الاستيراد الافتراضي

جملة_صدر         := 'صدّر' إعلان
                  | 'صدّر' '*' 'من' نص '؛'
                  | 'صدّر' '{' معرِّف {'،' معرِّف} '}' ['من' نص] '؛'
```

### ١٥.٤ الأنماط

```
نمط              := نمط_بسيط
                  | نمط_مصفوفة
                  | نمط_قاموس
                  | نمط_دالة
                  | نمط_اختياري
                  | نمط_معمم

نمط_بسيط         := 'عدد' | 'عدد_عشري' | 'نص' | 'منطقي' | معرِّف

نمط_مصفوفة       := 'مصفوفة' '<' نمط '>'

نمط_قاموس        := 'قاموس' '<' نمط '،' نمط '>'

نمط_دالة         := '(' [نمط {'،' نمط}] ')' ['->' نمط]
                    // حذف '->' مسموح فقط مع قائمة معاملات فارغة،
                    // وهو الشكل المختصر `()` أي دالة لا تُرجع قيمة

نمط_اختياري     := نمط '?'

نمط_معمم         := معرِّف '<' نمط {'،' نمط} '>'
```

### ١٥.٥ التعبيرات

```
تعبير            := إسناد

إسناد            := ثلاثي [('=' | '+=' | '-=' | '*=' | '/=' | '%=') إسناد]

ثلاثي            := أو ['?' تعبير ':' ثلاثي]

أو               := و {('||' | 'أو') و}

و                := مساواة {('&&' | 'و') مساواة}

مساواة           := مقارنة {('==' | '!=') مقارنة}

مقارنة           := حد {('<' | '<=' | '>' | '>=') حد}

حد               := عامل {('+' | '-') عامل}

عامل             := قوة {('*' | '/' | '%') قوة}

قوة              := أحادي {'**' أحادي}

أحادي            := ('!' | '-' | 'ليس' | '++' | '--') أحادي
                  | لاحق

لاحق             := أولي {استدعاء | فهرس | عضو | ('++' | '--')}

أولي             := حرفي
                  | معرِّف
                  | 'هذا'
                  | 'الأصل'
                  | '(' تعبير ')'
                  | حرفي_مصفوفة
                  | حرفي_كائن
                  | لامدا
                  | تعبير_جديد
                  | تعبير_انتظار
```

### ١٥.٦ الجمل

```
جملة_إذا         := 'إذا' '(' تعبير ')' كتلة ['وإلا' (جملة_إذا | كتلة)]

جملة_طالما       := 'طالما' '(' تعبير ')' كتلة

جملة_لكل         := 'لكل' '(' [إعلان_متغير | تعبير] '؛' [تعبير] '؛' [تعبير] ')' كتلة
                  | 'لكل' معرِّف 'في' تعبير كتلة

جملة_تطابق       := 'تطابق' '(' تعبير ')' '{' {ذراع_تطابق} '}'

ذراع_تطابق       := 'حالة' تعبير {'،' تعبير} '=>' (تعبير | كتلة)
                  | 'غير_ذلك' '=>' (تعبير | كتلة)

جملة_حاول        := 'حاول' كتلة ['التقط' '(' معرِّف ')' كتلة] ['أخيراً' كتلة]

جملة_إرجاع       := 'أرجع' [تعبير] '؛'

جملة_أوقف        := 'أوقف' '؛'

جملة_استمر       := 'استمر' '؛'

جملة_ارم         := 'ارمِ' تعبير '؛'
```

---

## ملحق أ: مرجع الكلمات المفتاحية

### قائمة الكلمات المفتاحية الكاملة

| الفئة | الكلمة المفتاحية | الوصف |
|-------|------------------|-------|
| المتغيرات | `متغير` | متغير قابل للتعديل |
| المتغيرات | `ثابت` | ثابت غير قابل للتعديل |
| الدوال | `دالة` | إعلان دالة |
| الدوال | `أرجع`، `ارجع` | جملة الإرجاع |
| الدوال | `متوازي` | دالة متوازية |
| الدوال | `انتظر` | تعبير الانتظار |
| التحكم | `إذا`، `اذا` | شرط |
| التحكم | `وإلا`، `والا` | فرع وإلا |
| التحكم | `طالما` | حلقة طالما |
| التحكم | `لكل` | حلقة لكل |
| التحكم | `في` | عامل في |
| التحكم | `افعل` | حلقة افعل-طالما |
| التحكم | `أوقف`، `اوقف` | إيقاف الحلقة |
| التحكم | `استمر` | استمرار الحلقة |
| التحكم | `تطابق` | مطابقة الأنماط |
| التحكم | `حالة` | حالة المطابقة |
| التحكم | `غير_ذلك` | الحالة الافتراضية |
| الكائنية | `صنف` | إعلان صنف |
| الكائنية | `ميثاق` | إعلان ميثاق |
| الكائنية | `يرث` | الوراثة |
| الكائنية | `يلتزم` | الالتزام بالميثاق |
| الكائنية | `عام` | رؤية عامة |
| الكائنية | `خاص` | رؤية خاصة |
| الكائنية | `محمي` | رؤية محمية |
| الكائنية | `مشترك` | عضو مشترك |
| الكائنية | `منشئ` | المنشئ |
| الكائنية | `هذا` | مرجع النسخة |
| الكائنية | `الأصل`، `الاصل` | مرجع الأب |
| الكائنية | `جديد` | إنشاء كائن |
| الاستثناءات | `حاول` | كتلة المحاولة |
| الاستثناءات | `التقط` | كتلة الالتقاط |
| الاستثناءات | `أخيراً`، `اخيرا` | كتلة أخيراً |
| الاستثناءات | `ارمِ`، `ارم` | رمي استثناء |
| الوحدات | `استورد` | استيراد وحدة |
| الوحدات | `صدّر`، `صدر` | تصدير إعلان |
| الوحدات | `من` | محدد المصدر |
| الوحدات | `كـ`، `ك` | محدد الاسم المستعار |
| الحرفيات | `صحيح` | صواب منطقي |
| الحرفيات | `خطأ`، `خطا` | خطأ منطقي |
| الحرفيات | `لا_شيء` | قيمة فارغة |
| المنطقية | `و` | و المنطقية |
| المنطقية | `أو`، `او` | أو المنطقية |
| المنطقية | `ليس` | النفي المنطقي |
| الأنماط | `عدد` | عدد صحيح |
| الأنماط | `عدد_عشري` | عدد عشري |
| الأنماط | `نص` | سلسلة نصية |
| الأنماط | `منطقي` | قيمة منطقية |
| الأنماط | `مصفوفة` | مصفوفة |
| الأنماط | `قاموس` | قاموس |
| الأنماط | `أي`، `اي` | أي نمط |

> **ملاحظة**: لا توجد كلمة `فراغ`. الدوال التي لا تُرجع قيمة تحذف ببساطة تحديد نمط الإرجاع.

---

## ملحق ب: سجل الإصدارات

| الإصدار | التاريخ | الوصف |
|---------|---------|-------|
| ١.٠.٠ | ٢٠٢٥ | المواصفات الأولية |

---

## ملحق ج: المراجع

١. ملحق معيار يونيكود رقم ٩: خوارزمية النص ثنائي الاتجاه
٢. المعيار التقني ليونيكود رقم ٣٩: آليات أمان يونيكود
٣. دليل مرجع لغة LLVM
٤. كتاب "صناعة المفسرات" لروبرت نايستروم

---

## ملحق د: مرجع رموز الأخطاء

### د.١ نظام رموز الأخطاء

تستخدم ترقيم نظام رموز أخطاء عربي موحد للتعريف المتسق بالأخطاء وتوثيقها. يتكون كل رمز من:

- **حرف الفئة**: حرف عربي يدل على فئة الخطأ
- **أربعة أرقام**: أرقام عربية-هندية (٠-٩) تحدد الخطأ المحدد

**التنسيق**: `[حرف][٤ أرقام]` (مثال: `د٠٣٠١`)

### د.٢ فئات الأخطاء

| الحرف | الاسم | الوصف |
|-------|-------|-------|
| ق | قراءة | أخطاء التجزئة اللغوية (أحرف غير صالحة، نصوص غير مغلقة) |
| ب | بناء | أخطاء بناء الجملة (رموز غير متوقعة، فواصل منقوطة مفقودة) |
| د | دلالة | أخطاء التحليل الدلالي (متغيرات غير معرفة، مشاكل النطاق) |
| ن | نوع | أخطاء نظام الأنماط (عدم تطابق الأنماط، فشل الاستنتاج) |
| ص | صنف | أخطاء متعلقة بالكائنية (الوراثة، الرؤية، المواثيق) |
| و | وحدة | أخطاء الاستيراد/التصدير (وحدات مفقودة، اعتماديات دائرية) |
| ت | توليد | أخطاء توليد الكود (أخطاء LLVM، فشل الربط) |
| ح | تحذير | تحذيرات المترجم (متغيرات غير مستخدمة، ميزات مهملة) |
| م | مهمل | تحذيرات صيغ مهملة |

### د.٣ أمثلة على رموز الأخطاء

| الرمز | الوصف |
|-------|-------|
| ق٠٠٠١ | حرف غير معروف |
| ب٠٠٠٢ | رمز غير متوقع |
| د٠٠٠١ | متغير غير معرف |
| د٠٣٠١ | استخدام 'أوقف' خارج حلقة |
| ن٠٠٠١ | عدم تطابق الأنماط |
| ص٠٠٠١ | صنف غير موجود |
| ح٠٠٠١ | متغير غير مستخدم |

### د.٤ استخدام أمر الشرح

للحصول على شرح مفصل لأي رمز خطأ:

```bash
tarqeem اشرح <رمز-الخطأ>

# مثال
tarqeem اشرح د٠٣٠١
```

يعرض أمر الشرح:
- وصف الخطأ بالعربية
- سبب الخطأ
- أمثلة كود توضح الخطأ
- الحلول والإصلاحات
- رموز الأخطاء ذات الصلة

### د.٥ تنسيق رسالة الخطأ

تتبع رسائل الأخطاء في ترقيم هذا التنسيق:

```
خطأ [رمز]: وصف الخطأ
  --> ملف.ترقيم:سطر:عمود
   |
 ن |     الكود المسبب للخطأ
   |     ^^^^
   |
   = ملاحظة: معلومات إضافية
```

### د.٦ التوثيق

التوثيق الكامل لرموز الأخطاء متاح في:
- `docs/رموز_الأخطاء/فهرس.md` - فهرس كامل لجميع رموز الأخطاء
- `docs/رموز_الأخطاء/نظام_رموز_الأخطاء.md` - مواصفات نظام رموز الأخطاء

---

**حقوق النشر ٢٠٢٥ مشروع ترقيم**

</div>
